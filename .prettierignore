**/bundle
**/coverage
**/dist
**/.git
**/node_modules
.docker
.DS_Store
.env
.gemini/
.idea
.integration-tests/
*.iml
*.tsbuildinfo
*.vsix
bower_components
# Generated by scripts/generate-changelog.js — do not hand-format.
CHANGELOG.md
eslint.config.js
**/generated
gha-creds-*.json
junit.xml
# Generated by pnpm; prettier reflows it into a shape pnpm does not emit,
# so formatting and `pnpm install` fight over the file. .yamllint.yml
# already ignores it for the same reason.
pnpm-lock.yaml
# Prettier re-pads every markdown table to column width. This file is the
# fork's deep reference and is read into model context at merge time, so that
# padding is ~15% of its tokens for nothing; `npm run check:tables` strips it
# with a proof that no cell changed. Without this entry `npm run format` pads
# it straight back and the gate fails, so the two would fight over the file
# exactly as they do over pnpm-lock.yaml above. Fork-owned, so opting it out
# costs nothing upstream — unlike AGENTS.md, whose padding prettier owns.
NO_TELEMETRY_GUIDELINES.md
# Same rationale for the path-gated fork rules: they were relocated verbatim out
# of QWEN.md/AGENTS.md to keep the always-on context budget under the warning
# threshold, and prettier re-padding a table added to one later would break that
# byte-for-byte relocation. Fork-owned, so opting out costs nothing upstream.
.qwen/rules/
.qwen/review-rules.md
Thumbs.db
packages/vscode-ide-companion/schemas/settings.schema.json
packages/cli/src/services/insight/templates/insightTemplate.ts
packages/cua-driver/
# The rest of eslint.config.js's global ignores, for the same reasons it
# gives. scripts/codemod/fixtures is the load-bearing one: those files are
# "intentionally non-idiomatic ink input/output", so normalising them to
# house style would corrupt the very data the codemod tests assert on.
scripts/codemod/fixtures/
packages/live-host/
packages/desktop-shell/runtime/
packages/core/src/skills/bundled/browser-use/runtime/
# Vendored from mobile-next/mobile-mcp and carries its own eslint config, so
# reformatting it to house style only widens the diff against upstream on the
# next sync. eslint.config.js already ignores packages/mobile-mcp/** for the
# same reason; .prettierignore had cua-driver but not its sibling.
packages/mobile-mcp/

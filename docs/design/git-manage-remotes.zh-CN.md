# Web Shell Manage Remotes（管理远程仓库）

[English](git-manage-remotes.md) | [简体中文](git-manage-remotes.zh-CN.md)

## 问题陈述

Web Shell 工作区的 git 弹出面板（`BranchPickerPopover`，从左侧栏工作区 git 胶囊按钮或 composer 分支 chip 打开）可以 pull、commit、push、创建/检出分支，并按 remote 名分组列出远程跟踪分支——但无法管理 remote 本身。从 zip 克隆（没有 `origin`）的用户、采用三角工作流（fork + upstream）的用户、或想删除过期 remote 的用户，都必须离开 Web Shell 去用终端。

目前不存在任何面向 git remote 的 daemon 路由、SDK 方法或 core CRUD 辅助函数（2026-09-06 经 grep 验证）。除此之外，`git remote` 还出现在：shell 只读分类器、worktree-service 的一条错误消息、`gitPush` 内部的 push-remote 解析，以及 `packages/cli/src/commands/review/` 下两个只读的 `git remote -v` 解析器（`lib/remote-match.ts`、`match-remote.ts`），再加上 `packages/core/src/extension/github.ts` 中 simple-git 的 `getRemotes`——这些先例说明渲染出来的 `git remote -v` 界面并不稳定（remote-match 已经带有一个针对部分克隆 `[filter]` 标注的回归测试），这也是下方新读取器直接读取仓库的配置 scope、而不读任何渲染输出或按名称访问器输出的又一个原因。

范围（已与维护者确认）：**list + add + remove**，以 **BranchPicker 弹出面板内的一个面板**形式呈现。不做 set-url、rename、fetch/prune。

## 现状

| 层            | 现有内容                                                                                                                                                                                                                                                                      |
| ------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| daemon routes | `packages/cli/src/serve/routes/workspace-git-branches.ts` 注册 `GET …/git/branches`、`POST …/git/{checkout,branch,push,pull,commit}`，同时有 legacy（`/workspace/…`）与 scoped（`/workspaces/:workspace/…`）两种形式；挂载于 `packages/cli/src/serve/server.ts` 约 L2403-2413 |
| core          | `packages/core/src/utils/git-branches.ts`：私有的 `runGit(cwd, args, env)`（execFile，30 s，10 MB，`gitEnv`），导出的 `gitEnv`、`fetchGitBranches`、`gitPush` 等；从 `packages/core/src/index.ts` 再导出（`export * from './utils/git-branches.js'`）                         |
| SDK           | `packages/sdk-typescript/src/daemon/DaemonClient.ts` 中的 `WorkspaceDaemonClient`（约 L6600-6770 的 git 区块）调用 `client.workspaceJsonRequest(selector, suffix, label, opts)`；类型位于 `packages/sdk-typescript/src/daemon/types.ts`，并在 `daemon/index.ts` 再导出        |
| UI            | `packages/web-shell/client/components/BranchPickerPopover.tsx` + `.module.css`；i18n 键 `branchPicker.*` 位于 `packages/web-shell/client/i18n.tsx`（en 约 L34-80，zh 约 L3523-3566）                                                                                          |
| e2e mock      | `packages/web-shell/client/e2e/utils/mockDaemon.ts`：scenario 字段（约 L106-120）、`isDaemonPath` 正则（约 L744-753）、`isDaemonRoute` 正则（约 L887-905）、响应 fixture（约 L1450-1593）                                                                                     |

仅 scoped 的先例：`workspace-github-prs.ts` 只注册 `/workspaces/:workspace/…` 路由（无 legacy 形式）。

## 变更方案

### 1. Core —— 新增 `packages/core/src/utils/git-remotes.ts`

```ts
export interface GitRemoteInfo {
  name: string;
  fetchUrl: string;
  pushUrl: string;
  extraFetchUrls: number;
  extraPushUrls: number;
  promisor: boolean;
  partialCloneFilter?: string;
  customRefspec: boolean;
  otherSettings: number;
}

export function isValidRemoteName(name: string): boolean;
export function isValidRemoteUrl(url: string): boolean;
export function isRemovableRemoteName(name: string): boolean;
export async function fetchGitRemotes(cwd, env?): Promise<GitRemoteInfo[]>;
export async function gitRemoteAdd(
  cwd,
  name,
  url,
  env?,
): Promise<GitRemoteInfo[]>;
export async function gitRemoteRemove(
  cwd,
  name,
  env?,
): Promise<GitRemoteInfo[]>;
```

- `fetchGitRemotes` 用单次 `git config --list --show-scope -z` 读取仓库**自身可编辑的 scope**，只保留 `local` 与 `worktree` scope 的记录。单独的 `git config --local` 比该 scope 更窄：`.git/config` 中一条 `include.path` 条目所贡献的键仍被 git 标记为 `local`，而 `extensions.worktreeConfig` 的 remote 存放在 `config.worktree` 中，因此仅 `--local` 的读取会少列条目、让 `git remote add` 走入死局（git 的重复检查会看到被 include 进来的名字）、并让 `git remote remove` 对一个拆分 section 报告成功（其被 include 的另一半仍然存活）。继承来的（`global`/`system`/`unknown`/`command`）remote 保持不列出（git 也无法删除它们，声称列出它们就等于为一次永远不会发生的删除背书）。读取配置而非 `git remote` + `get-url`，使列表不受 `insteadOf` 重写影响、对多值 `url`/`pushurl` 完整（以 `extraFetchUrls`/`extraPushUrls` 携带）、且没有 `git remote` + `get-url` 那种对每个 remote 两次的访问器扇出（列表本身是一次 git 进程调用；每个携带 promisor 的 section 额外一次 `--type=bool` 读取，此类 section 仅出现在 partial clone 中）。Section 附加信息（`promisor`、`partialclonefilter`、非默认 fetch refspec，以及作为 `otherSettings` 的其他 `remote.<name>.*` 设置计数）随结果一并携带，以便 UI 能说清删除会毁掉什么。`promisor` 的**值**由宿主机 git 自己解析（对每个携带 promisor 的 section 执行 `git config --type=bool --get-all`）：git 的整数布尔文法（strtoimax 拼写规则、单位因子、`maybe_bool` 界限）只能由宿主机 git 回答——在模块内维护一张解析表等于复制一个无法对齐版本的解析器——并且该键按累加方式读取（`--get-all`），因为 git 在 ANY 为 true 的记录上就会注册 promisor remote，而不是只看最后一条。记录以 NUL 分帧：在 `-z --show-scope` 下每条记录是 `scope\0key\nvalue\0`（无值的键是 `scope\0key\0`），这样带空格的 subsection 名和带转义换行的值都可解析。先运行一次 `rev-parse --git-dir` 探测：在仓库之外，其 stderr 携带 git 标准的 "not a git repository" 文本，路由分类器以此为准；而 `--list` 本身在未配置任何 remote 时会以退出码 0 返回仓库的其他键，所以空列表来自 scope 过滤。
- `gitRemoteAdd` / `gitRemoteRemove` 执行 `git remote add -- <name> <url>` / `git remote remove -- <name>`（`--` 终止符使删除能作用于 add 谓词所拒绝的、存在于配置中的名字），然后返回 `fetchGitRemotes`，因此每次变更响应都携带最新列表。Add 会先跨 ALL 配置 scope 对名字做预检：git 的 remote 家族的写入（add/remote 的重复检查、remove 的 section 编辑）只写仓库 scope（local、include 来源、worktree），而 fetch/push 跨所有 scope 解析名字——因此重复检查看不到继承来的（`global`/`system`/`unknown`/`command`）section，面板自己的 Add 否则会静默制造同名冲突：git 随后会从面板不显示的记录解析 fetch/push，多值 url 会同时推送到两个目的地。这种形态以 409 `remote_shadows_inherited` 拒绝（刻意的 shadow 属于终端）。删除运行镜像对称的预检，来自 ONE 份带 origin 标注的全 scope 转储（`--show-origin --show-scope`），在启动 `git remote remove` 之前完成：git 的 rm 在可能失败之前就会先删除跟踪引用并 unset 指向它的分支的键，所以任何表明该 section 有一部分活在删除所能编辑的两个文件之外的记录都必须预先拒绝——继承 scope 的记录（`global`/`system`/`unknown`/`command`）与仓库 scope 记录并存时（split-section 情形，git 会在毁掉 local 一半后以退出码 0 收场，而存活的一半继续让名字可解析）拒绝为 409 `remote_shadows_inherited`——仅存在于继承 scope 的 section 没有仓库一半可毁，因此落到 git 自己的 no-such-remote 404（客户端的过期行收敛正是以它为键）；origin 是 `include.path` 文件的仓库 scope 记录（scope 标签是 `local`，仅靠 scope 读取无法区分）拒绝为 409 `remote_section_in_included_file`。两种拒绝都不做任何改动。预检的「是否在仓库内」顺序来自其内部的读取：配置转储在仓库外会以退出码 0 返回（带着继承配置），但其 `rev-parse` 探测会在检查任何记录之前就在那里抛错，所以 git 标准的 not-a-repository 答案始终占优（add 路径钉死同样的顺序）。相对形式的 origin 相对 worktree 顶层目录（TOPLEVEL）解析（git 在 setup 期间 chdir），所以子目录 cwd 也能正常删除。删除之后验证该 section 已从仓库 scope 消失，AND 该名字完全不再解析——验证取两者的并集：全 scope 的 section 读取（一个继承来的 pushurl-only section 在 fetch 侧会以裸名解析，解析器看不见它）与 git 自己的解析器（`ls-remote --get-url`，从不接触远端）：任一 scope 中的配置幸存者、遗留的 `$GIT_DIR/remotes/<name>` 文件、`branches/<name>` 文件、或 insteadOf 别名，都会让一个「已删除」的名字仍可 fetch。推送侧 `url.<base>.pushInsteadOf` 前缀使裸名在删除后仍可解析的形状在**破坏前**即被拒绝，AND 认证的并集门从其 section 半所用的同一转储重读别名前缀（零额外 spawn），作为预检读取后竞入的兜底：破坏后拒绝会楔住认证清扫再也够不到的 upstream 键（重试的 404 converge 臂会跳过同一别名）——包括**在列表内**的仓库 scope remote 其名字被该别名前缀匹配（先删别名配置才能删 remote）；仅继承记录保持 404 教义，从未配置的别名形状名字亦然。别名本身也可能活在面板从不列出的继承 scope（global/system/unknown），成因于是落在工作区之外的文件里；且该 409 在任何 scope 下都不指名别名，拒绝在 UI 中始终没有可见成因——只要该别名前缀匹配名字，面板的任何操作都删不掉该行，fail-closed 教义下终端（删除别名）是唯一出口。FETCH 侧孪生（`url.<base>.insteadOf`）保持 house 既有的破坏后认证拒绝（预存行为、有钉死见证）：其 wedge——worktree scope upstream 键在 409 后存活、而重试的 converge 臂因同一别名跳过清扫——是 house 已知局限，本 PR 不改变。被拒绝的删除——包括被 include 一半存活的拆分 section——永远不会被报告为成功。有一个 scope 需要补救：`git remote remove` 无法编辑每个 worktree 自己的 `config.worktree`，并且会在删除跟踪引用 AFTER 才以 "Could not remove config section" 失败，于是一个 worktree scope 的行会被列出却永远无法删除，每次重试都重复破坏。当存活的 section ONLY 位于 worktree scope 时，删除用 `git config --worktree --remove-section` 完成收尾并重新验证；local scope 的幸存者继续回答 `remote_still_configured`（兜底——include 形态已被上面的预检拒绝）；任何其他 scope 的幸存者按删除本身失败的方式上报——`git remote remove` 本身失败时 git 的拒绝以 409 `git_config_write_failed` 呈现，git 退出码 0 但名字仍可解析时由全 scope 验证回答 `remote_still_configured`——并且在没有 `extensions.worktreeConfig` 时收尾永不运行（那里 `--worktree` 选择器会静默等同于 `--local`）。收尾也绝不会在 git 尚未做任何变更就失败时触发：git 会在其 fatal 行内逐字回显配置选出的 refspec 值，而该值可以携带真实换行，所以收尾只在行首匹配其拒绝短语，任何行首出现 `error:`/`fatal: invalid refspec` 都直接否决收尾——该拒绝有自己的答案（409 `remote_config_unparsable`；该行保留，以便用户修复 refspec），一次被收尾的「删除」会把它掩埋。剩下的 `Could not remove config section` 失败是配置写入失败——锁竞争，或一个根本不存在、无从改名的 section（上文遗留 `$GIT_DIR/remotes|branches` 形态，git 在 unset 分支键后以此消息作答）——以 409 `git_config_write_failed` 呈现。删除还会先清扫 git 的 rm 可能留下的孤儿跟踪引用——rm 只通过它能解析的 fetch refspec 删除它们，所以一次已认证的删除还会清扫无 refspec section 留下的 `refs/remotes/<name>/*` 命名空间（每个引用 `update-ref --no-deref -d`，归属按配置集合加被删名的最长前缀判定，含精确的裸引用——前缀兄弟的命名空间不动），并重新验证；no-such-remote 重试路径上同样收敛该清扫（该臂的清扫与其重新验证都只从结果中排除无命名空间的顶层 `refs/remotes/<name>`（归属解析保持完整，因此被已配置斜杠兄弟精确拥有的裸引用保留其归属）：这样的引用可能是从未配置之名的存活状态——remote-HEAD 符号引用、扁平 fetch 布局的无斜杠分支引用——清扫会在 404 之上毁掉它，重新验证计入它则会把 404 变成 409；认证路径保留裸引用，因为单目的地 fetch 会为已配置的 remote 留下它）。已接受残余：曾配置之名的裸引用残渣会被该排除孤立（先前尝试已删除 section 并在清理中途死亡；section 消失后两种状态不可区分，清扫会危及存活的那一个——与下文无 section 值残余同一权衡）。扁平布局的带斜杠分支引用不再是残余：清扫还跳过所有位于存活 remote 的 fetch 目标命名空间内的引用（读自 `remote.<r>.fetch`），无论其名字前缀为何都属于该 remote；通配符位于模式中部（`refs/remotes/*/main`、`or*/main`）的目标无法界定其命名空间，fail closed：整棵 refs/remotes/ 树视为外部，清扫在其中不删除任何引用（残渣作为缓存保留——安全极性）。删除还会清理 `git remote remove` 够不到的 upstream 键：git 只在它能写的文件里 unset 指向该 remote 的分支的 `branch.<b>.remote`、`merge` 与 `pushRemote` 键，并对多值键跳过且仅告警，因此存放在 `config.worktree` 的键——以及多值的 local 键——会存活成悬空的 `branch.<b>.remote = <gone>`（下次 pull 直接 fatal）。删除前快照记录指向该 remote 的分支——fetch 指向由 `branch.<b>.remote` 判定、push 指向由 `branch.<b>.pushRemote` 判定（各自按 worktree 覆盖 local、最后值胜出解析，因此实际上跟踪着某个 SURVIVING remote 的分支绝不被触碰）——在删除被确认后，unset local 与 worktree scope 的键（remote/pushRemote 条目用 `--fixed-value --unset-all`——多值键只贡献指名被删 remote 的条目——merge 用 `--unset-all`，且仅当同一 scope 的 remote 键不持有指名某个存活 remote 的条目时才动 merge——否则分支在那里保留其 upstream，merge 一半留下），并仅对按值匹配的键重新验证（merge 幸存者解析为 "." 且是惰性的，所以清扫无法编辑的、由 include 持有的 merge 键不得拒绝一次已完成的删除）。独立于指向快照之外，ANY 按值匹配到被删 remote 的 branch.<b>.remote/pushRemote 条目都要清除：多值键的非生效条目是残渣，日后浮现会成为悬空。链接 worktree（linked worktree）携带发起 worktree 的读取永远看不到的各自 config.worktree 文件，所以在对列出的兄弟路径做任何读写之前，清扫先验证该兄弟与本仓库共享 common dir（两端 `rev-parse --git-common-dir`，经路径拼写比较）：手工植入的 `.git/worktrees/<x>/gitdir` 可以指向无关仓库，而清扫会以它为 cwd 执行写操作；真实 `git worktree add ../feat` 兄弟通过（同一 common dir）。清扫遍历每个链接 worktree——`git worktree list --porcelain -z`，NUL 分帧字段——并清理它们——跳过 `prunable` 记录（目录已不在；无可读内容），并在某个兄弟 worktree 持有该名字的 per-worktree `remote.<name>.*` section 覆盖时拒绝（名字在那里仍可解析；属于面板不得摧毁的刻意状态），对每个存活 worktree fail-closed——同时容忍两种否定答案：尚无 `config.worktree` 的兄弟，以及 `extensions.worktreeConfig` 关闭时 git 的 `--worktree cannot be used` 拒绝（此时该文件在全仓库范围都是惰性的）。兄弟的 merge 键仅当同一文件的 remote 键正被按值清扫时才清扫——remote 存在共享配置里的 merge 不由这个文件决定。而 git rm 的生效值匹配可能删掉一个 LOCAL [branch <b>] section，其被遮蔽的副本指名的是某个 SURVIVING remote；删除前快照携带每个分支的 local 值列表，这些记录在 rm 之后立即恢复，BEFORE 一切事后门控（这样任何后续拒绝——门控侧或兄弟侧——都无法跳过对 git 自身破坏的回滚；rm 与恢复之间被 kill 是记录在案的残余风险——重试的新快照晚于破坏发生）。恢复也在删除失败路径的顶端运行（位于 `!completed` 抛出与收敛分类之上）：跟随在已破坏的 rm 之后的拒绝不得跳过回滚；被 kill 或失败的恢复 CONFIG 读取会重新抛出，而不是让 git 的原始回答——包括客户端过期行收敛所键控的 no-such-remote 404——在未回滚状态下浮现（fail-closed 掩蔽）；被 kill 或楔住的 gate 探针在 gate 内部回答不折扣（安全方向），而不是在 merge 臂之前中断回滚。只重写缺失的键，且「缺失」仅在 remote.<name> section 已离开发起 worktree 的 scope 链的 ALL scope（兄弟 worktree 的 per-worktree section 对此读取不可见——与 merge 翻涌并列的记录在案残余）AND 名字完全不再解析时折扣掉等于被删名的现存值——解析器探针（遗留 `$GIT_DIR/remotes/<name>` 文件、insteadOf 别名）加 local-path 探针（同名目录仓库或 bundle 文件：git 的传输无需任何配置记录即可应答，而 `--get-url` 从不查文件系统；无 section 值名字在路径探针之前短路——否则 scp 式拼写会被送上网络）（此时它是悬空残渣——不折扣则 include 持有的、指名被删 remote 的记录会永远挡住被毁 local 幸存者副本的写回）；section 仍存活时（git 可能在 unset 分支键之后死亡——陈旧引用锁），或遗留文件、别名、同名目录仓库使名字保持 LIVE 时，include 持有的等值记录仍是生效 upstream，刻意不折扣，回滚不会静默改指分支；exit-0 split-section 路径在其 worktree 半收尾 AFTER 才恢复，否则 gate 会看到收尾正要删除的 worktree scope 半。其余用快照捕获的同一集合来读：scope 转储把 include.path 文件的记录标记为 `local`，所以存在性检查读 `--local --includes`——裸 `--local`（关闭 includes）会把 include 持有的幸存者重新加进 `.git/config`，复制它并永远遮蔽 include（对缺失键的 `--add` 在 EOF 重建 section、越过 include 指令并在最后值解析中胜出；存活的 section 则接受段内插入，靠后的 include 记录仍可赢下 effective order 并使删除拒绝，fail-closed）：存活键回答 `remote_still_configured`，而不是确认出一个悬空状态，清理失败后的重试会收敛到 no-such-remote 路径而非走入死局。四个已接受残余与此回滚并列，均按后期轮次仅 Critical 的纪律记录在案而非修复：落在事后清理之后的拒绝（指向分支上 include 持有的、指名被删名的残渣）可能使认证后的 upstream 键清理再清扫掉刚恢复的 merge 键；以及 gate 对仅存在于兄弟 worktree config.worktree 的 section 给出的折扣——该文件在兄弟清扫之前对所有读取不可见，而兄弟清扫随后会拒绝该删除，拒绝之下的改指即为残余；第三个：当 git 的 rm 因无关原因（`rebase`/`description` 一类它不动的键）使 local `[branch <b>]` section 存活、且 `[include]` 指令位于其后时，写回的 `--add` 做段内插入、落在指令之前，残渣保持 effective order，删除永久拒绝——被准予的折扣被吞没（fail-closed：HEAD 同样拒绝这些形状且丢失更多状态）。第四个仅竞态且预存（没有任何设计闭合对并发写者的 TOCTOU）：落在并集门与尾部存活键门之间的继承 `remote.<name>.*` 记录会被认证——尾部门只检查 `remote.pushdefault` 与 `branch.*`，而 `readUpstreamContext` 在该点已持有新鲜的全 scope 转储，故若将来要收窄该窗口，一次 `remote.<name>.` scope 复查的额外 spawn 为零。push 指向的分支只失去其 pushRemote——它的 merge 键属于其存活的 FETCH upstream。解析到被删 remote 的 local 或 worktree scope `remote.pushDefault` 以同样方式清扫，因为 `git push` 否则会解析到已消失的 remote。存放在 git rm 无法写入的文件中的同类 upstream 键——`include.path` 文件（scope-local）或继承的 global/system 文件——也会存活为同样的悬空状态，而这些文件不在本模块会编辑的范围内，所以幸存者拒绝为 `remote_still_configured` 而不是被清扫。幸存者检查按 git 的方式解析每个 `branch.<b>.remote`/`pushRemote` 与 `remote.pushDefault`（scope 链上最后值胜出），所以被更高优先级 scope 中指向 SURVIVING remote 的记录所遮蔽的记录永不拒绝——git 的 rm 自己在 unset 前也比较生效值——而其 rm 所 unset 的 `.git/config` 遮蔽副本可能把一个同值的继承记录 UNMASK 成活生生的悬空状态，解析检查能抓到这一点。unmask 检查同样遵循删除前快照：一个被指向的分支，其遮蔽记录被删除 unset 后，可能回退到一个继承记录，其指名的 remote 在任何地方都没有存活 section（更早已删除，或在此从未存在）——分支的 upstream 从被删 remote 变成了悬空名，所以删除拒绝而不是为之背书。这里的「解析」是 git 自己的答案，而不是记录集合：裸词值用 `ls-remote --get-url` 探测（从不接触远端；仅在没有任何东西应答时逐字回显名字），因为一条无 URL 的 `[remote "foo"] proxy = …` 记录什么也解析不出来，而遗留的 `$GIT_DIR/remotes/<name>` 文件在没有任何记录的情况下也能解析——记录集合在两个极性上都是错的。探测是 FETCH 侧的（`ls-remote --get-url` 看不到 pushurl-only 的 section），所以两个 PUSH 侧分支（分支 pushRemote、remote.pushDefault）还把已取转储中的 `remote.<value>.pushurl` 记录或 `url.*.pushInsteadOf` 别名前缀计为可解析——仅 push 侧的 upstream 在 fetch 侧悬空但 push 侧可解析。以裸词命名 worktree 内目录仓库的值也计为可解析——git 的路径传输能应答它而解析器看不到——经 `ls-remote -- <值>` 探测（git 自己的传输按魔数识别目录仓库与 bundle，与扩展名无关；name 键的认证门从不做此探测：碰巧同名的目录不得让一个已配置的 remote 变得不可删）。只检查快照指向的条目——被指向的分支 AND 删除前的 `remote.pushDefault` 解析：快照证明它们的值 WAS 被删 remote，所以变化的值就是这次删除造成的——别处的既有悬空 upstream 不由这次删除来拒绝。git 无需 section 即可解析的无 section 值（本地仓库的 `.`、URL、scp 风格 `host:path`）在探测之前短路（这些形状要么会把探测送上网络、要么无需探测）；其余 git 读作本地传输的值——裸词、无冒号路径形状、以及冒号在首斜杠之后的路径（git 仅在冒号先于首斜杠时识别 scp 式拼写）——落到解析器 + 路径探测（win32 上的 UNC 任意混合分隔符拼写除外——探测会经 SMB 阻塞；win32 上 NTFS 拒绝其尾部的盘符 url 亦除外（含 `aux.txt`、`lpt0` 等保留名前缀，依 git 的前缀规则）——git 将其路由到 ssh；NTFS 合法的盘符路径在 win32 上是本地传输、会到达探针，而其他平台将任何盘符形状读作 scp-like ssh 并由冒号规则回答），因此存在的路径仍认证、而悬空的带斜杠值（`ghost/fork`、不存在的相对路径）拒绝，与裸词孪生始终相同的极性。同样的值也以「不予处理」的方式被拒绝作为删除目标：no-such-remote 收敛分支（section 已消失的重试）以名字是否可能是 section 名为门，所以 `remove(".")` 回答 git 的 404，而不会清扫按值匹配到 `.` 的存活跟踪键。裸词名在事后是可区分的，所以该闸门还会咨询 git 自己的解析器：一个没有任何 section 却仍解析的名字（insteadOf 别名——经探测会以 no-such-remote 失败且 git 不动任何东西）同样跳过清扫；worktree 内以裸词命名的目录仓库（解析器看不到的存活本地路径 upstream——经 `ls-remote -- <名>` 探测）也跳过清扫；push 侧别名（`url.*.pushInsteadOf`，即 `gh:` 形态）让裸名在 push 侧可解析而 fetch 侧无任何应答，此时由转储本身回答（git 没有 push 侧解析器探测）；遗留的 `$GIT_DIR/remotes|branches` 文件则会让 git 自己的 rm 在 unset 分支键之后因 section 缺失而失败（git 的自身行为，如实呈现——不在收敛分支内）。收敛清理会像认证路径一样重新验证引用清扫（幸存的引用——残留的锁——会被拒绝，而不是把该命名空间遗弃给 404），并运行清扫的 snapshot 作用域 unmask 门（被清扫 unshadow 的悬空继承记录拒绝）；其全 scope 存活键半不在此臂——快照从未指向的继承 branch 键幸存者在「无任何认证发生」时是预存状态，git 的 404 仍是客户端过期行收敛的答案（被清扫 unshadow 的快照指向分支、或继承的 `remote.pushDefault` 指名被删名时，仍经解析检查的对应臂在此拒绝 409）。该跳过的已接受残余：git 同样接受 `.` 形或 scp 式 SECTION 名（冒号先于首斜杠），因此一个手工创建的、名字为这些形状且形如无 section 值的 remote，若首次尝试的清理在清扫中途夭折，重试时不会收敛任何内容——两种状态（从未有 section 的存活 upstream 与删除失败留下的孤儿）在 section 消失后无法区分，而清扫有毁掉存活配置的风险。
- `runGit` 仍是唯一的 exec 封装：从 `git-branches.ts` 导出它（加一个关键字）并在此处导入；`gitEnv` 已经导出。
- `isValidRemoteName` 刻意保守——单个 refname 风格组件：复用 `isValidRefName`（控制字符/空格与 `~^:?*[\` 拒绝、点/`.lock`/`..`/`@{` 规则、每组件上限——含对 `HEAD` 的拒绝：git 自己的 remote 名检查会接受它，拒绝它是刻意的保守，由谓词表钉死），并追加 `no /`、`no leading -`（flag 注入防护）与不可见字符类（渲染后与现有 remote 完全一样的名字是一个删除欺骗面）。git 在 `remote add` 时会再验证一次；这个谓词的存在是为了让路由能在启动 git 之前回答 400。
- `isValidRemoteUrl` 接受 git 接受的几乎所有东西（https/ssh/scp 风格/本地路径，含空格），拒绝空白、前导 `-`（exec 向量）、一般形式的会执行命令的 transport-helper 形态（锚定的 `<scheme>::` 前缀，无字母开头规则且允许空 scheme——`7z::archive` 会执行 `git-remote-7z`，`::sh -c id` 不在 git 的默认拒绝清单里，无覆盖时 git 会执行 PATH 上解析到的 `git-remote-`（而 `ext::` 默认被拒）；protocol.allow 策略虽能覆盖二者，但该策略可被配置文件改写），以及不可见字符类。helper 拒绝是必要的，即使对 git 默认策略就拒绝的形态亦然（空名形态不在其中）：该策略可被配置文件覆盖，任何按调用清洗环境变量的手段都够不到，也可被 `GIT_ALLOW_PROTOCOL` 环境变量覆盖，后者由 `gitEnv` 通过归一化中和——继承来的列表在剥掉会执行 helper 的条目（`ext`、`fd`）后保持其默认拒绝效力，过滤成空的列表保持已设置状态（全部拒绝）而不是变成 undefined。前缀锚定在开头，所以 IPv6 字面量（`ssh://git@[::1]/repo.git`）或 scp 风格路径永不匹配；原生 git 不带 `git-remote-ssh`，所以普通的 ssh/scp 风格/ssh:// URL 从不携带 `::` 分隔符，保持被接受。
- 不可见字符类派生自 Unicode 属性（`\p{Cc}` C0/C1 控制字符、`\p{Cf}` 格式字符、`\p{Default_Ignorable_Code_Point}`，加上行/段分隔符 U+2028/U+2029），而不是手工清单——属性随 Unicode 增长，枚举永远存在未列出的角落。
- `isRemovableRemoteName` 是删除谓词，刻意比 add 谓词**更宽松**：删除针对的是 git 已经配置的 remote（手工编辑的 `.git/config` 可以持有 add 谓词拒绝的名字，包括被清洗器剥离的不可见字符名），所以它唯一的底线是非空、execFile 无法在 argv 条目中携带的 NUL 字节、以及不含 `/`（带斜杠名字的跟踪命名空间会与前缀 remote 的冲突，清扫无法区分归属——见 §2）。因此 `HEAD` 是合法的删除目标，尽管 add 谓词拒绝它（add 侧经共享 `isValidRefName` 继承分支名拒绝——git 自己的 remote 名检查会接受 `HEAD`，add 侧的拒绝是刻意的保守，由谓词表钉死）；收敛臂对裸引用的排除正是让 `HEAD` 删除不会清扫存活的 remote-HEAD 符号引用的原因（见 §2）。`--` 终止符守护其余一切的 exec 向量，而不是靠拒绝名字。配置键永不为空且永不携带 NUL，所以 `/` 底线是列出行唯一可能失败的底线（带斜杠的 remote 会被列出但拒绝删除——终端的 `git remote remove` 仍是它的工具，见 §2）；路由仍会对客户端提供的名字应用该谓词（在任何 git 启动之前回答 400 `invalid_remote_name`）。
- 新文件**不**从 `packages/core/src/index.ts` 再导出：cli 路由深路径导入 `utils/git-remotes.js`（#11798 规则禁止 cli 新增从 core barrel 的值导入），本特性最初加的那行 barrel 导出已移除。

### 2. Daemon 路由 —— 新增 `packages/cli/src/serve/routes/workspace-git-remotes.ts`

仅 scoped（GitHub-PRs 先例；Web Shell 始终经由 `workspaceByCwd`）：

| 方法 | 路径                                       | 请求体          | 成功                                                                |
| ---- | ------------------------------------------ | --------------- | ------------------------------------------------------------------- |
| GET  | `/workspaces/:workspace/git/remotes`       | —（`?cwd=` 可） | `{ v: 1, workspaceCwd, available: true, remotes: GitRemoteInfo[] }` |
| POST | `/workspaces/:workspace/git/remote`        | `{ name, url }` | `{ v: 1, workspaceCwd, remotes: GitRemoteInfo[] }`                  |
| POST | `/workspaces/:workspace/git/remote/remove` | `{ name }`      | `{ v: 1, workspaceCwd, remotes: GitRemoteInfo[] }`                  |

- 注册形态镜像 `registerWorkspaceQualifiedGitBranchRoutes`：`registerWorkspaceQualifiedGitRemotesRoutes(app, { workspaceRegistry, sendBridgeError, mutate })`，在 `server.ts` 中直接挂载在 branch 路由挂载之后。
- 请求流水线与 branch 变更完全一致：`deps.mutate({ strict: true })` → `resolveTrustedRuntime` → `generationGuard.assertOpen()` → `resolveContainedCwdOrFail`（GET 使用非致命的 `resolveContainedCwd`）。
- 在任何 git 启动之前做请求体验证：`name` 必须满足 `isValidRemoteName`（→ 400 `invalid_remote_name`）；`url` 在 trim 后必须满足 `isValidRemoteUrl`（→ 400 `invalid_remote_url`）。Remove 用宽松的 `isRemovableRemoteName` 验证，并让未知名字浮现 git 自己的 `error: No such remote`（见下）而不是预检，使路由对并发 CLI 使用无竞态。宽松谓词拒绝的唯一形态是带斜杠的名字：其跟踪命名空间是前缀 remote 命名空间的子目录（`refs/remotes/origin/staging/*` 位于 `refs/remotes/origin/*` 之内），所以任何清扫都无法区分该 remote 自己的引用与前缀 remote 的分支引用——终端里的 `git remote remove` 仍是干这活的工具。
- 错误映射：`workspace-git-branches.ts` 中共享的 `sendGitError`（已导出；其 `redactGitMessage` 辅助函数保持私有）新增七个 remote 专属分支，位置在**每个关键字分支之前**，因为 git 在这些消息中逐字回显用户选择的 remote 名（以及配置写入失败时的 URL）。每个分支 ONLY 在组合后 detail 的开头——第 1 行——匹配；config-write 分支额外接受 git 文档化的两行 `could not lock config file …` 链（lock 行是 git 自己的，不是回显内容）。表中的模式按 git 自己的大小写转写，而交付的代码大小写不敏感地匹配——这是为宽松遗留关键字分支预留的双保险余量（这些分支匹配截断到上限的切片——无界扫描会把超过上限的、URL 中携带关键字的长 push/pull 输出重新归类），针对的是同一分类器携带的关键字形态，而不是声称 git 会改变这些消息的大小写：配置选出的名字可以携带任何关键字，配置选出的值（URL 或 fetch refspec）可以携带真实换行——git 会反转义带引号值中的 `\n`——所以任何更深的行首文本都是攻击者可控的，绝不可认领：
  - `^(?:error|fatal): Could not remove config section ` /
    `^(?:error|fatal): could not set 'remote\.` /
    `^(?:error|fatal): could not unset 'branch\.` → 409
    `git_config_write_failed`（配置写入失败——锁竞争，或 section 位于 git 不愿编辑的文件——而非脏工作区；`git remote remove` 在删除引用和移除 section 之前先 unset 指向它的分支的 `branch.*.remote` 键，所以锁可能以三种形态中的任何一种浮现）
  - `^(?:error|fatal): remote .+ already exists\.?\s*$` → 409
    `remote_already_exists`
  - `^(?:error|fatal): No such remote: ` → 404 `no_such_remote`
  - `^remote still configured after removal$` → 409
    `remote_still_configured`（删除验证失败——存活的 section，OR 清理无法 unset 的 worktree upstream 键，OR 仍从 git rm 无法写入的文件（`include.path` 文件或继承的 global/system 文件，包括被 unmask 的同值继承记录）RESOLVING 到被删 remote 的 upstream 键，OR section 消失后名字仍可通过 git 自己的解析器解析（遗留的 `$GIT_DIR/remotes/<name>` 文件、`branches/<name>` 文件、insteadOf 别名）、OR 推送侧 `url.<base>.pushInsteadOf` 前缀使名字仍可解析（**破坏前**拒绝），OR 兄弟 worktree 的 config.worktree 持有被删 remote 的键——或 per-worktree section 覆盖；该消息不携带名字，所以没有任何关键字分支可以认领它）
  - `^remote section lives in an included config file$` → 409
    `remote_section_in_included_file`（remove 预检拒绝；无 git 前缀的普通 Error，在 `git remote remove` 运行之前抛出，所以没有任何东西被破坏）
  - `^(?:error|fatal): invalid refspec` → 409 `remote_config_unparsable`（git 在变更任何东西之前解析配置的 fetch refspec 时就死掉：行保留，无破坏，原因可指名）
  - `^remote already configured in an inherited scope$` → 409
    `remote_shadows_inherited`（add AND remove 的预检拒绝——remove 在启动 git rm 之前运行同样的继承 scope 检查，因为 git 会在毁掉 local 一半后以退出码 0 收场——事后只有模块自己的全 scope 验证会拒绝；无 git 前缀的普通 Error）
    remotes 路由原样委托给它；其他一切（not-a-repo → 404 `not_a_git_repository`、脱敏后的 500 兜底）保持现有分类。影响面：该分类器被所有 git 路由共享，所以该表由属主文件里一个同目录的 `sendGitError` 分类表测试钉死（消息进 → 状态/码出），包括携带关键字的 remote 名与被分类 detail 的 stdout 一半。

### 3. SDK —— `packages/sdk-typescript`

- `types.ts`：`DaemonGitRemoteInfo { name; fetchUrl; pushUrl; extraFetchUrls; extraPushUrls; promisor; partialCloneFilter?; customRefspec; otherSettings }`、`DaemonGitRemotesResult { v; workspaceCwd; available; remotes }`、`DaemonGitRemoteMutationResult { v; workspaceCwd; remotes }`（变更响应回答最新列表，不带 `available`）；从 `daemon/index.ts` 再导出。
- `WorkspaceDaemonClient`（仅 scoped，紧邻 `workspaceGitBranches`）：
  - `workspaceGitRemotes(cwd?)` —— GET `…/git/remotes`
  - `workspaceGitRemoteAdd(name, url, cwd?, timeoutMs?)` —— POST `…/git/remote`
  - `workspaceGitRemoteRemove(name, cwd?, timeoutMs?)` —— POST
    `…/git/remote/remove`（变更方法带按调用超时：面板传入共享的 remote 变更 fetch 超时，其链路长于 30 秒默认值）
- 无 legacy `DaemonClient` 方法（不存在 legacy 路由）。

### 4. Web Shell UI —— `BranchPickerPopover.tsx`

- 新状态 `view: 'branches' | 'remotes'`，打开时重置为 `'branches'`；进入 remotes 视图时清除 pull 解析面板，**仅当它仍在场**（`pullBlocked`）——常驻的 stash 恢复警告在往返中存活，遵循 `stickyWarningRef` 的契约（有测试钉死）。在场警告在进入时做快照、退出时恢复（消息 AND sticky 标志），因为 remotes 变更自己的底部消息（`Added remote …`）否则会覆盖 stash 条目在产品内的唯一记录并解除该标志——在返回按钮 AND 视图仍打开时的关闭（下次打开时恢复）两处恢复，两者都以无进行中变更为门，这样落定中的变更自己的底部消息不会被砸掉、留存的快照能活到更晚的打开——并且在场警告永远优先于留存快照（打开时已激活的标志就是快照所持有的同一警告——视图打开时的关闭永不解除它——或一条更新的警告；无论哪种，在场消息胜出、留存快照被丢弃）。
- 新操作行 **Manage Remotes…**（lucide `GlobeIcon`），位于第二操作组，_Checkout Tag or Revision…_ 之后；也被现有 `actionsVisible` 搜索过滤匹配。
- Remotes 视图替换可滚动列表区（搜索框与底部状态栏保留）：
  - 头部行：返回箭头 + 标题；吸附在滚动盒顶部，进入视图时聚焦，切换永不把焦点掉到 `document.body`。离开视图把焦点还给打开面板的那一行。Escape 先离开 remotes 视图（第二次 Escape 才关闭弹出面板），所以「返回」这个键不会毁掉已输入的 add 草稿。弹出面板打开期间，一个 window 捕获阶段的 `preserveImeEscape` 掩码（镜像 `DialogShell`）对 Radix 捕获阶段的 dismiss 监听器隐藏 IME 所属的 Escape——它只检查 `event.key`——所以取消输入法组合会话的 Escape 既不会拆掉视图，也不会通过该层的 preventDefault 吞掉原生取消。
  - Remote 行：名字（其 DISPLAYED 形式——按 CSS 着墨方式清洗、折叠空白并修剪两端——为空的名字渲染为可见的 `(invisible name)` 标签；显示形式仅与原始值 DIFFERS 的名字——包括与兄弟行文本撞车的、以及仅差空白（CSS 会把它从着墨文本中折叠掉）的——再加上类清单无法枚举的四个结构性分支：非 NFC 的名字（其正则等价的孪生着墨完全相同）、混入另一种文字字母的拉丁名（西里尔 `о` 同形字形态）、携带 U+FFFC/U+FFFD 的名字（可见的 Common 脚本替换 glyph，隐形类与混文字臂都漏掉它们）、以及与兄弟行发生 TR39 skeleton COLLISION 的名字——每个名字先经内置 vendored `unicodeConfusables` 原型表折叠（表的原型是权威答案：NFKC 会把希腊 lunate sigma 路由到 Σ 而失去表回答的 C），表未覆盖的兼容性形态回退到 NFKC，并以**入口 NFC 规范化**加收尾 NFC 使正则等价的骨架相等（仅收尾 NFC 做不到：落在预组合字符原型内部的附加符号会与其基字符粘住，而规范排序在分解拼写中把它前移，两种拼写于是重组出不同的栈——NFC 是规范函数，每种规范拼写进入同一代表元）——该遍迭代到不动点（带上限）：收尾 NFC 可能合成一个表键（`0` 按表到 `O`，随后 NFC 把 `O` + U+030B 合成 Ő → Ö），分解写法也只在合成之后才遇到表（表由 `packages/web-shell/scripts/generate-confusables.mjs` 从 Unicode 的 confusables.txt 生成；生成器还把每个产出的原型在同一条运行时折叠——入口 NFC、表优先（含对入口 NFC 从「表基+附加符号」融合出的码点取「重组后为表键的最长分解前缀」）、NFKC 回退、NFC 收尾——下闭包，使携带表未覆盖兼容性半形的值（`%` → `º/₀`：其半形 NFKC 为 `o/0`，逐半表机会再把 `0` 提为 `O`）不会把同一个着墨类劈成两个 skeleton；已知局限：confusable 基加额外组合附加符号（K+M 对其原型拼写 P+M）在折叠后附加符号落在相同组合类时仍可能劈开——统一该类需要组合类感知的比较空间，列为 follow-up；入口 NFC 再生成的已接受权衡：六个原先统一的着墨类（如 `unicodeConfusables.ts` 第 51 行的 `ņ` U+0146 对 `n̦` = n+U+0326，以及同文件第 372/505/508/1654/1665 行的西里尔/阿拉伯/拉丁/希腊形状）现在折叠到**不同**的 skeleton，故该形状的近乎相同名字不再被标记——过合并对同形字标记器是安全方向，但旧折叠产生的正则等价劈裂——4853 个 NFC 与 NFD 拼写不同的 key+mark 组合（每个表键 × U+0300–U+036F）中比较 NFC 拼写的折叠与 NFD 拼写的折叠所得的 21 个，新折叠下为零——是更大的缺陷，新折叠将其全部闭合且在该语料上零回归，记录在 `remote-name-skeleton.test.ts` 的见证旁），skeleton 等于兄弟而原始名不同的行会被标记：Latin/Common 内部着墨完全相同的孪生（`ofﬁce` 连字、无点 `ı`）能通过每个按属性的分支，折叠用一条表规则收拢整个类，而不是每个家族一条分支。对携带非 ASCII 成员的组，碰撞标记携带非规范拼写的行（raw ≠ skeleton——异常字符即可见证据），所以普通兄弟——以及没有孪生的单个合法非 ASCII 名——保持不标记——除非组的骨架本身非 ASCII 且差异来自表折叠而非正则等价：那里不动点行（原型文字孪生——阿拉伯 ةة 对拉丁 öö）同样不携带可见异常，与全 ASCII 情形同一证据缺失，因此两行都标记（纯正则差异——NFD/NFC 孪生——保持单行极性）。碰撞组键是**净化后**名字的 casefold skeleton（剥掉不可见字符，与行显示及搜索折叠臂同值，故一个不可见字符无法把真正的碰撞组劈成单元素组；组成员标志——全 ASCII、规范、case-twin 计数——也读净化名）——折叠本身保持大小写敏感（其 `I`→`l` 命中喂给搜索的三种大小写形式），但 casefold 作用于 skeleton 的**输出**——大小写敏感表命中仍区分类（`ORIGIN` 折叠为 `ORlGlN`，自成一类）——故该类在这些命中之外是大小写不敏感的：`0rigin` 与 `origin` 碰撞、且作为全 ASCII 组两行都标记；skeleton 仅大小写不同于原始名的行是其类的规范拼写，而非冒充者；仅大小写差异的组（任意文字——`café`/`CAFÉ`、`ẞ`/`ß`）两行都标记，与全 ASCII 臂同一证据失败（可见名字文本仍是仅大小写情形的区分者；ASCII 大小写下转义尾无字符可拼）；对全可打印 ASCII 的组，`raw ≠ skeleton` 不携带「哪个拼写是冒充者」的证据（表的 `m → rn` 展开让合法的 `main` 成为看起来异常的一侧），因此两行都标记，而转义为空操作的原型拼写行不获得转义尾。碰撞计数在 UNFILTERED 列表上计算，所以把某个孪生单独筛出来的搜索无法剥掉幸存者的标记；行搜索把标记骨架作为额外的名字侧匹配目标，因此仅表折叠的孪生（无点 ı、长 s）可以按其着墨文本被找到——以 needle 的每种大小写形式（表大小写敏感：`I`→`l` 存在而 `i`→`l` 不存在，因此 `Istanbul` 无论大写、小写或混合输入都必须能到达其 `lstanbul` 孪生）。被标记的行追加 `(hidden characters)` 标记——或者，对仅因可打印 ASCII 内部碰撞（表把 1 折叠为 l、m 折叠为 rn；没有隐藏字符）或仅因占位臂（U+FFFC/U+FFFD 是**可见**替身，而非隐藏字符）而被标记的行，追加 `(lookalike name)` 标记——其 tooltip 与 aria-label 携带原始名并把可区分字符拼成可见码位转义（canonical/script 分支拼出每个非 ASCII 字符；skeleton 行还拼出折叠覆盖的可打印码位，使 tooltip 永远可区分），所以撞脸行永不呈现普通行的身份）、一个说明删除除 URL 外还会毁掉什么的徽标（partial-clone/promisor、自定义 refspec、额外配置的 URL、其他 `remote.<name>.*` 设置的计数）、fetch URL（截断显示，带 `title` 属性；push URL 仅在不相同时显示）、以及末尾的删除图标按钮。
  - Remove 使用**两次点击内联确认**（第一次点击把行按钮换成红色 _Confirm_ 状态，其可访问名仍携带该 remote 的显示名；第二次点击执行；任何其他操作或视图切换都重置它）——弹出面板内不嵌套对话框。待命状态有自己的 focus-visible 样式，因为基础 hover 颜色已被待命颜色用掉。
  - 列表底部的内联 add 表单，吸附且可换行，使 Add 控件保持在定宽弹出面板内：名字输入框 + URL 输入框 + Add 按钮（任一输入框内 Enter 提交，按本仓库惯例防御 IME 所属的 Enter——`isComposing` 或 keyCode 229——所以组合输入非 ASCII 名不会把预提交文本提交出去；变更进行中输入框禁用；客户端防护只拒绝空值与前导 `-`——daemon 仍是验证权威，其 400 消息落在底部状态栏）。
  - Remote 名与 URL 通过派生自 Unicode 属性的显示清洗器渲染（非用户所著的 `.git/config` 可能携带使两行渲染得完全一样的不可见字符），搜索过滤的两侧都按 CSS 着墨方式清洗 AND 折叠空白，外加 NFKC 折叠（连字着墨为其字母对），所以从行显示文本复制的搜索词能找到该行；变更请求仍使用原始名。URL tooltip 在转义不可见字符的同时也转义空白，所以仅差空白的两个 URL 的 tooltip 永不相同。
  - 搜索输入框在该视图中过滤 remotes 并携带 remotes 专属占位符；进入或离开视图都清空查询，所以用来找到操作行的字符串不会把面板过滤成空。
- 数据流：进入视图时 fetch `workspaceGitRemotes(gitCwd)`；add / remove 直接使用变更响应返回的列表。只有 **remove** 随后在后台调用 `fetchBranches(true)`——`git remote remove` 删除 `refs/remotes/<name>/*`，所以 branches 视图的 remote 分组必须在用户返回前刷新；add 不创建远程跟踪引用，所以不刷。被 404 `no_such_remote`、409 `git_config_write_failed` 或 `remote_still_configured` 拒绝的 remove 也刷新分支列表与状态：git 在写 section BEFORE 就删除跟踪引用（以及指向它们的分支的跟踪配置），所以任何证明或留下该破坏的拒绝——另一个客户端先删了 remote、写入锁失败、或拆分 section 的另一半在验证后存活——否则会让屏幕上留下已消失的引用。**被拒绝的**变更**静默**重读 remotes 列表（无加载占位、无错误替换），且**仅当** daemon 的错误码表明列表已过时（`remote_already_exists`、`no_such_remote`、`remote_still_configured`）；其他一切拒绝（400 验证、503 draining、传输失败）保留可用行与已输入草稿在屏幕上，只通过底部状态栏发声。变更错误落进现有底部状态栏（`statusType: 'error'`）。
- 焦点：进入视图聚焦返回按钮；离开恢复 manage-remotes 行（其被禁用时恢复搜索框）。为进行中的变更禁用 add 输入框或删除按钮会在真实浏览器中使聚焦控件失焦，成功的删除会把聚焦的行整体卸载，所以焦点在变更落定时恢复：成功 add 后恢复到名字输入框，否则恢复到记住的字段或提交按钮。删除在行存活时恢复该行的删除按钮——包括静默重读使其保留的被拒删除——在行消失时恢复面板返回按钮，无论删除成功还是被拒删除所等待的重读把它收敛掉了。若用户在进行中把焦点改借到另一个内容内控件（搜索框从不禁用），落定恢复跳过：恢复不得把焦点从用户有意移到的控件上拽走。聚焦对话框容器本身（Chromium 在 inert chrome 上 mousedown 会聚焦 Radix FocusScope 的 tabIndex=-1 content）不算改借控件，恢复仍在那里运行。每次捕获与查找都从弹出面板内容自己的根解析（`getShadowAwareActiveElement` / 以根为作用域的 `querySelector`）：在 shadow-portal 嵌入中 `document.activeElement` 会重定向到宿主，`document.body` 查找无法跨越边界，否则整个恢复在那里都是惰性的。
- i18n（en + zh），跟随现有 `branchPicker.*` 区块：`branchPicker.action.manageRemotes`、`branchPicker.remotes.title`、`.empty`、`.noMatches`、`.loading`、`.namePlaceholder`、`.urlPlaceholder`、`.add`、`.back`、`.remove`、`.removeConfirm`、`.removeConfirmFor`、`.invisibleName`、`.hiddenChars`、`.lookalikeName`、`.searchPlaceholder`、`.urlTooltipFetch`、`.urlTooltipPush`、`.promisor`、`.partialClone`、`.customRefspec`、`.extraUrls`、`.otherSettings`、`.added`、`.removed`、`.invalidInput`（空值/前导横杠输入的客户端防护；daemon 仍是验证权威）。
- CSS：`BranchPickerPopover.module.css` 新增类（`.remotesHeader`、`.backButton`、`.remotesTitle`、`.remoteRow`、`.remoteName`、`.remoteBadge`、`.remoteUrl`、`.remoteRemove`、`.remoteRemoveConfirm`、`.addRemoteForm`、`.addRemoteButton`），构建在同样的 `--border` / `--muted-foreground` / `--destructive` 变量之上；头部与 add 表单在滚动盒内吸附，表单以 `min-width: 0` 输入框换行，不会溢出定宽弹出面板。

### 5. e2e mock —— `packages/web-shell/client/e2e/utils/mockDaemon.ts`

- 新增可选 scenario 字段 `gitRemotes?: DaemonGitRemoteInfo[]`（默认 fixture：一个完整形态的 `origin`），按 `gitBranches` 的方式穿线。
- 在 `isDaemonPath` 与 `isDaemonRoute` 的 git 交替分支中扩展 `remotes|remote(?:/remove)?`（仅 scoped 路径，与路由匹配），让新路径留在 daemon 路由表上。
- 响应处理器：GET 返回 fixture 列表；POST add/remove 修改内存列表并返回它（镜像真实契约），带 `remote_already_exists` / `no_such_remote` 错误体。

### 6. 测试（与源码同目录）

| 文件                                                                | 覆盖内容                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `packages/core/src/utils/git-remotes.test.ts`                       | 配置读取列表契约（配置顺序、经 `git remote set-url --push` 的 push-url 覆盖、promisor/部分克隆 remote、无 url 的 section → 空 url、多值 url、NUL 分帧下带空格的 subsection 与内嵌换行的值、继承 scope 排除、`insteadOf` 免疫、invalid-refspec 存活、`otherSettings` 计数），仓库 scope 用例（include.path 列表、split-section 与 include-held-section 的预检拒绝（保持引用与分支键完好、重试回答完全一致）、worktree scope 列表与 worktree scope 删除收尾（含带点兄弟门控）、`git-remotes-kill.test.ts` 中的 killed/无匹配配置读取区分与被杀读取的 stdout 转储剥离（含被杀的 promisor 徽标读取、被杀的解析器读取、被杀的 worktree-list 读取、被杀的兄弟读取、被杀的恢复读取）、继承 scope 的 add 拒绝与 remove 拒绝（含两侧各自的 not-a-repo 顺序见证）、被继承遮蔽的 worktree 门控，以及 upstream 键清理阶段——指向分支的删除前快照（worktree 覆盖 local、最后值），然后确认后清理 local 与 worktree scope 的 `branch.<b>.remote`/`merge`/`pushRemote` 键（remote/pushRemote 用 `--fixed-value --unset-all`，merge 用 `--unset-all`，重新验证并拒绝），含 local section 清理用例、多值精确值用例、混合 scope merge 用例、push 指向的分支只失去 pushRemote、worktree scope pushDefault 清扫、植入锁拒绝、no-such-remote 重试收敛、include 持有的 upstream 键与 pushDefault 拒绝、按精确值清扫的多值 LOCAL 键，以及继承 scope upstream 键用例（global scope 拒绝、rm-unmasking 拒绝、由删除前快照钉死的 unmasked-gone-remote 拒绝与 unmasked-surviving-remote 接受、被存活 remote 遮蔽的接受、pushDefault unmask 三件套（gone-remote 拒绝、surviving-remote 接受、pre-existing-dangling 接受）、pushRemote unmask 拒绝、带斜杠名字的删除拒绝（带斜杠的跟踪命名空间与前缀 remote 的冲突）、无 section 值的接受（本地仓库伪 remote `.`、被 unmask 的 URL upstream、被 unmask 的 scp 风格 pushDefault、被 unmask 的本地路径 upstream）、未知 scope 幸存者拒绝（在 `git-remotes-kill.test.ts` 中）、include 持有的惰性 merge 接受、被存活 worktree 记录遮蔽的 include 持有残渣接受，以及子目录 cwd 删除、兄弟 worktree 清扫（含 prunable 记录跳过、扩展关闭的接受、section 覆盖拒绝、共享配置 merge 保留、多值存活条目 merge 保留、无关子键容忍）、被毁 local 副本的恢复（含多值幸存者 merge 恢复与 kill 套件顺序见证——恢复落在 rm 之后、每个事后门控之前）、遗留 .git/remotes 拒绝、pushurl-only 继承幸存者与 same-name-in-global 拒绝落在任何破坏 BEFORE（local section、跟踪引用与分支键全部完好，含 worktree section 形态）、无 section 收敛门（`.`、scp 风格、insteadOf 别名与裸词目录仓库目标回答 git 的 404 且存活跟踪配置不被触碰）、仅在继承 scope 中的 section 回答 git 的 404（没有仓库一半可毁）、裸词目录仓库的 unmask 接受（含子目录 cwd 形态——路径探测经由 git 自己的传输回答、相对顶层目录）、bare 布局的 `<name>` 拼写、pushRemote/pushDefault 镜像、bundle 文件对（git 的 `ls-remote` 传输按魔数识别 bundle、与扩展名无关——被它取代的 `--resolve-git-dir` 模型曾以 "too large to be a .git file" 拒绝）、空别名守卫（空 pushInsteadOf 值会前缀匹配所有名字）、pushInsteadOf 别名名的收敛跳过与 unmasked pushRemote 接受（push 侧别名由转储回答——没有可用的解析器探测）、收敛臂的被杀路径探测读取（kill 套件）、其持续引用锁拒绝（重新验证的清扫），以及两个仅竞态可达的兜底（并集门的 section 半与 worktree 收尾的 `size !== 1` 合取，kill 套件）、被 unmask 的无 URL section 拒绝与遗留文件接受（unmask 门探测 git 自己的解析器而非记录集合）、pushurl-only pushDefault 接受（push 侧分支把 pushurl 记录计为可解析）及其 pushRemote 臂对（pushurl-only 与存活 remote 两个接受）、include 持有的 pushDefault 不重复（恢复的存在性检查读 `--local --includes`）、两端空白 AND 尾部 CR 名字的往返、带点兄弟覆盖接受、被遮蔽 pushDefault 的恢复、收敛分支的兄弟清扫、非生效多值残渣清扫，以及存活条目 merge 保留）），宿主机 git promisor 委托（仅版本稳定拼写、多值累加读取、不可解析值底线、跨 scope 键存在性）、invalid-refspec 收尾否决（注入精确前缀拒绝行，section 存活）、无 refspec 删除的孤儿跟踪引用清扫（worktree 与 local 形态、带点兄弟命名空间保留、符号引用在不解引用下清扫、清扫失败后的收敛臂重试）、精确裸引用 `refs/remotes/<name>` 与带斜杠兄弟命名空间保留（按配置集合加被删名字的最长前缀判定归属）、无 pushurl 的 push 扇出计数、撞脸 add 拒绝、invalid-refspec 拒绝上的 section 存活断言、scoped worktree 一半断言、仅 Default_Ignorable 的 url 行、on/OFF 关键字行、宿主机 system-config 前置条件守卫，tmp 仓库中的 add/remove 往返（含 dash/TAB/谓词合法名字）、谓词表（含 add/remove 宽松度分叉与 NUL 底线）、trim 后 url 存储、启动前的 helper-URL 拒绝、错误路径回滚见证（陈旧引用锁毁掉 local section）、收敛臂裸引用排除（remote-HEAD 符号引用、扁平布局、已配置斜杠兄弟的裸引用）、被 kill 的错误路径恢复读取（kill 套件）、外部目标命名空间清扫排除（存活 refspec 目标指向被删名字命名空间，含扁平目标）、空值 pushDefault 不重复（NUL 分帧存在性读取）、悬空带斜杠/反斜杠 unmask 拒绝、converge 臂 snapshot 作用域 unmask 拒绝（被清扫 unshadow 的悬空继承 upstream）、converge 臂对清扫从未触碰的继承键的 404 教义、冒号在首斜杠之后的悬空 upstream 拒绝、include 持有残渣的恢复（等于被删名的现存值仅在 section 已离开 ALL scope AND 名字完全不再解析时被折扣——遗留 `$GIT_DIR/remotes/<name>` 文件、insteadOf 别名、或同名目录仓库或 bundle 文件会使其保持 LIVE（解析器探针不查文件系统，故 gate 携带 local-path 腿）；无法回答的探针给出不折扣而非中断回滚——覆盖 remote、pushRemote 与 pushDefault 三处存在性读取，且 exit-0 split-section 路径仅在 worktree 半收尾 AFTER 才恢复——使被毁 local 幸存者副本得以写回，且 ref-lock 使 section 存活或遗留文件、别名、目录仓库使名字仍解析时不回指分支或 push upstream——由两条 ref-lock 见证、legacy 不改指一对、目录仓库与 bundle 不改指一对、pushDefault 与 pushRemote 写回见证、探针失败 merge 恢复、split-section 收尾写回（断言认证）、pushurl-only section 的 scope 腿见证、kill 套件对 scp 式被删名永不触及路径探针的 pin、以及 kill 套件对被杀 gate 探针给出不折扣而由后续门呈现 kill 的 pin、与多值全名 merge 恢复见证（未折扣 `remoteNowRaw` merge 条件的唯一 pin）、按平台的盘符见证（win32 拒绝悬空本地路径；POSIX 认证且零 ssh spawn）钉死）、以及七个 pushInsteadOf 预检见证（local section 拒绝且 section 与指向键完好存活、严格前缀拒绝、仅继承记录的 404 教义、worktree scope 拒绝、继承 scope 别名对列表内仓库 scope section 的拒绝、included 与 inherited 拒绝先于 alias 拒绝的顺序）、并集门 push 别名兜底见证（预检读取后竞入的别名拒绝）与折扣 gate push 别名不折扣见证） |
| `packages/cli/src/serve/routes/workspace-git-remotes.test.ts`       | 镜像 `workspace-git-branches.test.ts`：信任门、代数守卫（三个端点全部 503 `workspace_runtime_unavailable`）、`invalid_cwd` 400、`invalid_remote_name`/`invalid_remote_url` 400、add→list、重复 → 409 `remote_already_exists`、remove→list、remove-missing → 404 `no_such_remote`、配置锁 → 409 `git_config_write_failed`（section 写入链与跟踪分支 unset 链两种）、非仓库 → 404 `not_a_git_repository`（祖先上溯上限）、未知工作区 → `workspace_mismatch`、路径脱敏、remove 的 NUL 名 400、按 POST 注册的严格变更门控、变更中途 generation 关闭 → 503、运行时环境穿线进 git（来自 fixture-global section 的继承遮蔽拒绝）、环境 remote-section 守卫的行边界 pin（两段式 `remote.pushDefault`——朴素、后接带点键、或值中带点——不得读作具名 section，而带点 section 名必须命中）、system 守卫的宽度（继承 `remote.pushDefault` 是硬拒绝触发器），以及宿主机 system-config 前置条件守卫；`workspace-git-branches.test.ts` 中同目录的分类表钉死每个 `sendGitError` remote 分支（含 included-file 预检拒绝），包括对关键字分支的优先级、携带关键字的 remote 名、脏名字 remote 形态、detail 的 stdout 一半，以及 linked-worktree 主 gitdir 脱敏（NUL 与头部截断的目标、继承的 home/XDG 配置文件（含空 XDG、无 HOME 与尾部斜杠逐字拼接形态）、构建期 system-gitconfig 路径、任意位置 include 目标形态（POSIX 绝对路径、Windows 盘符反斜杠 AND 正斜杠、UNC 负载）、线性词元 /etc/gitconfig 分支（20 万字符无空白串远在钉死的 10 秒上界内回答——其区分的二次方形态实测约 17 秒）、截断键前缀分支的粘附尾见证（粘附在回显键上的 100k 无空白尾整体脱敏）、fail-closed 绝对路径清扫（未枚举的 `bad numeric config value … in file %s` 形态被脱敏、带撇号的 include 目标保持前缀脱敏、transport-URL 对照组逐字存活））                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `packages/sdk-typescript/test/unit/DaemonClient.test.ts`            | 三个新方法的 URL/方法/请求体组合，含与不含 `cwd`，以及 add 与 remove 的按调用超时透传（每次 abort 都与一个永不落定的 fetch 竞速）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `packages/web-shell/client/components/BranchPickerPopover.test.tsx` | 打开面板 → 列表渲染；add 成功/失败；本地 dash 防护；remove 两次点击确认 + 计数的删后刷新 + `onBranchChanged`；返回按钮；搜索过滤；过滤为空的文案；加载失败渲染；常驻警告往返存活；重开重置/解除；Escape 先离开视图（反关闭守卫的可取消事件见证）；退出时焦点恢复到 manage 行、变更禁用该行时回退到搜索框；`(invisible name)` 标签与待命确认的 aria-label；撞脸行标记（含 URL tooltip）；删除后果徽标；仅 stale-list 错误码触发静默重读；重读进行中保留行；still-configured 重读；config-write-failure 与 still-configured 与 no-such-remote 的分支刷新；焦点恢复（落定后的 add 表单字段、成功删除后的返回按钮、shadow-portal 根的删除恢复）、空白撞脸名标记、正则等价与混文字撞脸标记（合法非 ASCII 不标记）、TR39 skeleton 碰撞标记（连字、无点 ı 与长 s 孪生带码位转义 tooltip 被标记——长 s 一对钉死 table-first 的折叠顺序——规范兄弟与单个非规范名保持朴素）、可打印 ASCII 碰撞标记为 `(lookalike name)` 且拼出折叠覆盖的码位、经 NFKC 折叠的搜索按连字行的着墨文本找到该行（且隔离搜索下标记存活）、消费者折叠劈裂见证（产出原型中表未覆盖的 NFKC 半形在生成期重新闭包，使 `ᵒ` 与 `º` 留在同一个 skeleton）、URL 同形字与 skeleton 碰撞共燃时保留隐藏字符标记（标记优先级，fetch/push 两臂）、非 ASCII URL 的 fail-closed 标记（URL 是纯 ASCII 界面）、多行底部消息的句边界、IME 所属 Enter 的 add 防护、IME 所属 Escape 掩码（不拆视图、不关闭、原生取消不被抑制、键在输入框之前恢复）、常驻警告跨 remotes 变更恢复、视图打开时的关闭、落定/更新警告竞态、过滤掉/Escape/add 提交时解除待命确认、加载状态正见证、进入时清除 pull 面板、底部错误清洗、非等待的分支刷新、add 时清空过滤、extras 文本搜索、搜索词清洗、push 不同 tooltip（经 i18n 键本地化，含 zh-CN 见证）、指名后果的待命 aria-label、删除中行 spinner、视图进入聚焦、经 mock 的 DismissableLayer 对等实现的未拦截 Escape 关闭、显示清洗与原始名删除、操作查询隔离、搜索焦点保留、工作区切换焦点重置与陈旧丢弃、静默重读失败保留行与已输入草稿、进行中 add 输入框禁用、被拒删除行卸载焦点恢复、跨弹出面板焦点隔离、工作区 cwd 读取作用域，以及焦点测试共享的打开自动聚焦落定、消费者折叠劈裂见证（表未覆盖的 NFKC 半形在生成期重新闭包）、非 ASCII 骨架表折叠碰撞双行标记、拒绝后刷新先于被等待的重读发出（await 中途切换 workspace）、混合大小写同形字孪生经 casefold 碰撞组键标记（全 ASCII 组→两行、lookalike 文案）、U+FFFC/U+FFFD 占位臂在 lookalike 文案下带码位转义 tooltip 标记、仅大小写不同的原型对（`ẞ`/`ß`）与非 ASCII 仅大小写孪生（`café`/`CAFÉ`）由**逐对** case-twin 臂 3 标记（非 ASCII 第三成员不得解除 ASCII 仅大小写对的标记）、臂 4 由原型文字孪生钉死、臂 1 的单行极性由混合大小写规范孪生钉死、大写规范孪生（`CAFÉ`/`CAFE\u0301`）保持单行极性、改借焦点跳过（remove 与 add 两路径上进行中聚焦搜索框保持焦点）、inert chrome 焦点恢复（对话框容器 tabIndex=-1 焦点不取消落定恢复）、不可见字符成员标志在三行碰撞中保持 clean twin 被标记（sanitized 碰撞的 aria-label 更新为 `Remove origin (lookalike name)`）、组键净化见证（不可见字符藏于碰撞伙伴内部时 clean twin 仍被标记）、仅 all-ASCII 与仅 case-twin 成员标志见证、以及 sanitized 渲染查表见证（skeleton 转义尾拼出折叠覆盖的码位）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `packages/web-shell/client/utils/unicodeConfusables.test.ts`        | 表完整性门：无自映射、值不含表键、每个键折叠到其值且每个值是运行时折叠的不动点、所有值为 NFC                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `packages/web-shell/client/utils/remote-name-skeleton.test.ts`      | TR39 折叠的不动点角落：NFC 合成的表键（`0` + U+030B → Ő → Ö）与分解写法仅在合成后遇到表，外加对每个表键与值的幂等性，以及大小写敏感钉（skeleton 保留表的大小写——`0rigin` 折叠为 `Origin` 而非 `origin`——因为大小写不敏感位于 picker 的 casefold 碰撞组键）、多码位名字的规范等价不变性（入口 NFC）、被融合基字符的 table-first 规则（重组后为表键的最长分解前缀）、以及 base×mark 扫描不变性（已知局限：相同组合类的「confusable 基+附加符号」拼写仍可能劈开——follow-up）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `packages/web-shell/client/e2e/web-shell.git-remotes.spec.ts`       | Playwright + mockDaemon：侧栏胶囊 → 面板 → list/add/remove（两次点击，`@smoke`）、重复 add 错误（含提交按钮焦点恢复）、搜索过滤、弹出面板裁剪区内 add 表单几何，以及 15 行压力 fixture 钉死裁剪区内行/删除按钮宽度与两个滚动极端处的吸附头部/表单——所有结构性选择器用 `data-testid`（带哈希的 CSS-Modules 类子串在兄弟 local 共享前缀时会过度匹配）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |

## 受影响文件

- `packages/core/src/utils/git-remotes.ts`（新增）+ `.test.ts`（新增）+
  `git-remotes-kill.test.ts`（新增——killed/无匹配配置读取判别器）
- `packages/core/src/utils/git-branches.ts`（导出 `runGit`；`gitEnv`
  归一化继承的 `GIT_ALLOW_PROTOCOL` 而非删除它）+
  `git-branches.test.ts`（环境隔离与归一化见证）
- `packages/core/src/index.ts`（不变——无 barrel 再导出；路由深路径导入该模块）
- `packages/cli/src/serve/routes/workspace-git-remotes.ts`（新增）+ `.test.ts`（新增）
- `packages/cli/src/serve/routes/workspace-git-branches.ts`（导出 `sendGitError`；共享分类器新增七个 remote 专属分支）+ `workspace-git-branches.test.ts`（同目录的 `sendGitError` 分类表）
- `packages/cli/src/serve/process-env-guard.test.ts`（为 redaction 读取守护进程自身 `HOME`/`XDG_CONFIG_HOME` 登记守卫豁免，按文件与按访问计数——不是无条件豁免）
- `packages/cli/src/serve/server.ts`（挂载新的注册函数）
- `packages/sdk-typescript/src/daemon/types.ts`、`daemon/index.ts`、`daemon/DaemonClient.ts`
- `packages/sdk-typescript/test/unit/DaemonClient.test.ts`
- `packages/web-shell/client/components/BranchPickerPopover.tsx` + `.module.css` + `.test.tsx`
- `packages/web-shell/client/i18n.tsx`
- `packages/web-shell/client/utils/unicodeConfusables.ts`（新增——vendored UTS #39 confusables 表，生成产物，eslint 豁免：类型感知规则在 6.5k 条目的字面量上 OOM）+ `packages/web-shell/scripts/generate-confusables.mjs`（新增——其再生成脚本；拒绝 HTTP 错误或条目数过小的来源，而不是覆写该表）
- `packages/web-shell/client/utils/remote-name-skeleton.ts`（新增——TR39 折叠，迭代到不动点）+ 其同目录测试（不动点角落、幂等性）
- `eslint.config.js`（为生成表新增一条 ignore 条目）
- `packages/web-shell/client/e2e/utils/mockDaemon.ts`
- `packages/web-shell/client/e2e/utils/gitScenario.ts`（新增——与 `web-shell.git-mode.spec.ts` 共享的 git 工作区 fixture）+ `web-shell.git-remotes.spec.ts`（新增）+ `web-shell.git-mode.spec.ts`（使用共享 fixture）+ `capture-git-mode-screenshots.ts`（迁移到共享 fixture）

## 范围边界

- 仅 list + add + remove。不做 set-url、rename、fetch、prune 或 LFS/URL 重写 UI。
- 仅 scoped 路由；不做 legacy `/workspace/git/remote*`。
- 不改 `GitDialog`；唯一入口是 BranchPicker 弹出面板。
- daemon 侧无缓存——每次 GET 重新读 git（廉价，且对并发 CLI 使用始终准确）。

## 安全说明

- 所有 git 调用经由 `runGit`（execFile 参数向量，无 shell）；变更用 `--` 终止选项，使配置中的名字永不可能被读成 flag，`url` 拒绝 C1 控制字符与不可见字符类，是为了不让面板写入一个与兄弟行着墨相同的 URL（配置卫生；变更请求与 git 自己的输出仍携带原始值）。渲染边界的控制是显示清洗器，它同时覆盖写入谓词永远看不到的配置既有/继承 URL。
- 会执行命令的 transport helper（锚定的 `<scheme>::` 形态（含空 scheme 的 `::payload`），如 `ext::`、`fd::`）被**写入谓词拒绝**。git 的默认策略（`protocol.ext.allow=never`）在连接时同样拒绝它们，但该策略可被配置**文件**覆盖（包括全局 `[protocol] allow = always`——`file://` clone 被拒时的标准手工修复），任何按调用的环境清洗都无法移除它，所以默认策略本身不是保证。环境变量覆盖（`GIT_ALLOW_PROTOCOL`）通过归一化而非删除来中和：继承的列表在剥掉会执行 helper 的条目后保持默认拒绝效力，因为直接删除会把最终决定权交给工作区控制的 `protocol.<name>.allow`。前缀按 git 的 transport 名字符集锚定在开头，所以 IPv6 字面量或 scp 风格路径永不匹配；原生 git 不带 `git-remote-ssh`，所以 `::` 分隔符把普通 URL 形态（ssh://、scp 风格、https）与 helper 形态分开，拒绝它不破坏任何合法 remote。
- 不可见字符类在写入谓词与显示清洗器两侧都派生自 Unicode 属性（`\p{Default_Ignorable_Code_Point}`）而非枚举，因为集合随 Unicode 增长，手工清单永远存在未列出的角落（Tag 区块 U+E0000-E0FFF 零宽零墨，所以一个未列出的码位会让两个不同的 remote 渲染得完全一样）。
- 变更继承完整严格链：变更门 → 可信运行时解析 → 代数守卫 → 工作区包含的 `cwd`。
- 错误响应经过现有脱敏（`redactGitMessage`），绝对路径永不到达客户端。脱敏替换工作区路径、git 根，AND 任何位于 cwd 树之外的 gitdir——linked worktree 共享的主 `.git` 目录（git 在配置锁失败时回显该绝对路径——`could not lock config file /srv/main/.git/config`）或位于上层项目 `.git/modules` 下的子模块 gitdir——从 git 根处的 `.git` FILE 解析（cwd 可能是子目录），包括相对 `gitdir:` 值，外加 gitdir 内 git 自己的 `commondir` 文件（迁移过的管理目录没有名为 `worktrees` 的父目录）。`.git` 文件用刻意宽松接受的文法解析（git 只接受同行目标；脱敏绝不可对 git 可能解析的形态少接受），两次读取都有头部限长——这些文件是 daemon 共享错误路径上的工作区可控内容。构建期 system gitconfig（`<prefix>/etc/gitconfig`，Homebrew/源码构建位置）按空白分隔词元脱敏——无界 `\S*` 前缀分支会在一个长无空白串上耗费 O(L²) 的同步事件循环 CPU（被拒 push 的 sideband 数据绕过 git 的 `vreportf` 上限），在 512 字符切片生效之前。截断 gitdir 头的前缀分支（读取头在目标中途截断）采用同样的按词元纪律——indexOf 定位字面键并消费匹配词元——粘附的无空白尾不会留在线上。而 git 的 config-error 家族是渲染形态的开放集合（`bad numeric/boolean/date config value … in file %s`，随版本措辞漂移），枚举没有最后的角落：`in file` 分支以该家族的共享短语为键，脱敏到行尾（带空格的 include 目标不留尾巴），最后的 fail-closed 清扫移除任何其他句子中任何存活的绝对路径词元——其空白词元边界是形态分支所拥有的记录在案的边界，清扫永不越界；引号路径分支（git 的 `die(_("'%s' …"))` 家族，如 `fatal: '<path>' does not appear to be a git repository`）整体拥有含空格的引号负载（含内部撇号负载的片段分支）。transport URL 在两者中都存活：其斜杠跟在 scheme 的 `:` 或单词字符之后，永不落在清扫的前缀类上。在此之上，配置读取自己的失败路径在错误离开 core 之前剥离其 stdout：被杀的 `git config --list --show-scope -z` 可能携带 EVERY scope 记录的部分转储（global/system URL、凭据 helper、身份），只有 git 的 stderr 诊断属于客户端可见消息。
- 删除经过验证：`git remote remove` 之后重读仓库 scope 列表，存活 section 抛错，AND 检查名字跨 ALL scope 的解析——同名继承（`global`/`system`/`unknown`/`command`）幸存者会在一次看似成功的删除之后继续接收 push，所以该形态同样抛 `remote_still_configured`。两侧都预先拒绝同样的冲突（`remote_shadows_inherited`——只要存在可毁的仓库 scope 一半，删除侧就拒绝；git 的重复检查看不到继承 section，而 git rm 会在毁掉 local 一半后以退出码 0 收场），事后验证则作为预检之后竞入幸存者的兜底。仅由 worktree scope 持有的幸存者在那里完成收尾（`git config --worktree --remove-section`），因为 `git remote remove` 无法编辑 `config.worktree`，否则会在毁掉其引用后留下一个永远无法删除的列出行。删除谓词的唯一底线是非空、NUL 字节（execFile 拒绝携带 NUL 的 argv 条目，否则会浮现为未分类的 500）与不含 `/`（带斜杠名字的跟踪命名空间与前缀 remote 的冲突）；其他所有配置中的名字保持可删除，`--` 终止符守护 exec 向量。

## 待决问题

无阻塞项。评审时待核实的假设：remove 用两次点击内联确认（vs. 模态框）——选择它是因为 Radix Dialog 嵌套在 portal 化的 Popover 内会与 dismiss 层打架；该模式与弹出面板现有的 discard-and-update 内联确认一致。

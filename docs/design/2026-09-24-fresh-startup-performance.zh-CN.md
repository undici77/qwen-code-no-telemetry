# 全新启动性能：可输入时间与 RSS

[English](2026-09-24-fresh-startup-performance.md) | [简体中文](2026-09-24-fresh-startup-performance.zh-CN.md)

状态：已实现。第 9 节记录了设计决定。

## 1. 问题

在干净的机器上，`qwen` 大约需要 2.5 秒用户才能开始输入。前约 2 秒终端一片空白，整个进程树占用约 575 MB RSS。`qwen -p` 大约需要 2.1 秒，第一个模型请求才离开进程，即使对端是即时应答的本地模型服务也是如此。

此前没有任何度量覆盖这些问题。现有的启动剖析器在最后一个进程内部、模块加载完成之后才开始计时。它报告约 1.8 秒到达 `input_enabled`，而用户实际等待约 2.8 秒；也没有 CI 任务跟踪交互式启动。

## 2. 方法

我们遵循 [How we made claude.ai 3x faster in two weeks](https://claude.dev/blog/how-we-made-claude-ai-faster/)（Anthropic，2026）中的做法。其原则在本工作中的应用如下：

1. **度量用户能感知的时刻，取 p75。**用户感知的时刻是"输入的按键显示在输入框中"，而不是某个内部标记。
2. **能度量的问题才能被解决。**每一段慢的环节都先被追踪，再用一个改动可以推动的数字把它呈现出来，然后才动手修改。
3. **墙钟时间是真相，但噪声太大，不适合做门禁。**因此用确定性代理指标作为支撑：进程数、加载的 JS 字节数，以及 Valgrind 下的 CPU 指令数。每个代理指标在采信之前都与墙钟时间做过对照。
4. **不稳定或不相关的指标一律丢弃。**见第 8 节。
5. **防范"可用"之后的不稳定。**文章中是页面加载后的布局偏移，这里对应可输入之后屏幕仍在变化。

### 2.1 干净环境

- **安装。**`corepack pnpm install --frozen-lockfile`，然后构建并打包。Node 22、Linux、4 vCPU。
- **每次运行使用全新状态。**每次运行都新建 `HOME`、`QWEN_HOME`、`QWEN_RUNTIME_DIR`、`XDG_*` 和 `TMPDIR` 目录。工作区是一个空的 git 仓库，`settings.json` 只包含认证类型和模型名。环境变量只保留 `PATH`、`LANG` 和 `TERM`。
- **模型。**一个即时应答的本地回环 OpenAI 兼容桩服务。
- **入口。**真实的 `qwen` bin 入口 `scripts/cli-entry.js`。
- **终端。**120×40 的 node-pty 终端，屏幕由 `@xterm/headless` 重建。它像现代终端一样应答 DA1 与 OSC 11；"静默"变体不做任何应答。

### 2.2 指标

| ID  | 指标                      | 类型               | 定义                                                                                      |
| --- | ------------------------- | ------------------ | ----------------------------------------------------------------------------------------- |
| J1  | 交互式 TTI                | 墙钟，p75          | 从启动 `qwen` 起，到 "Type your message" 可见、已输入一个按键、且该按键显示在输入框中为止 |
| J2  | 无头 TTFR                 | 墙钟，p75          | 从启动 `qwen -p` 起，到第一个请求到达模型服务为止                                         |
| J3  | 首个内容                  | 墙钟，p75          | 从启动到第一个可见字符（即屏幕保持空白的时长）                                            |
| M1  | TTI 时的 RSS              | 内存               | TTI 时刻进程树的 RSS 总和；`-p` 取退出前的峰值 RSS                                        |
| S1  | TTI 后稳定性              | 比率               | 在没有任何输入的情况下，TTI 之后屏幕仍发生变化的启动所占比例                              |
| D1  | TTI 前 Node 启动次数      | 确定性（strace）   | TTI 之前启动的 Node 进程数                                                                |
| D2  | TTI 前加载的 JS 字节数    | 确定性（strace）   | 读取的 JS 字节数，对所有进程求和                                                          |
| D3  | CPU 指令数，`-p` 直到退出 | 确定性（Valgrind） | 在关闭 ASLR、V8 `--predictable --hash-seed=1 --random-seed=1`、编译缓存已预热的条件下测得 |

**代理指标验证：**

- **D3 与墙钟时间一致。**在 `--version`、`--help`、`-p` 以及缓存预热后的 `-p` 上，D3 与墙钟时间的 Pearson 相关系数 r = 0.9997，重复运行的结果相差在 0.008–0.04% 以内。
- **D3 看不到等待。**终端从不应答 OSC 11 探测时，墙钟时间增加 165 毫秒，但指令数没有增加。

### 2.3 实验流程

- **每个实验一个假设。**每个假设的依据来自 CPU 剖析、strace、esbuild metafile 或屏幕差异。
- **交错 A/B。**每个改动都与其前一版本按 ABBA 交错成对运行：20–30 对，每次运行使用全新 HOME。报告成对差值的中位数，以及改动在多少对中胜出。
- **噪声下限。**将两个版本放在相同目录深度下做 A/A，未见偏差：成对中位数约为 0，胜率约各半。第一次 A/A 尝试曾暴露出 24 毫秒的路径深度偏差，采用等深布局后消除。
- **保留或舍弃。**只有推动了指标的改动才被保留。任何指标出现回退，都先查明根因再继续。

## 3. 基线：时间花在哪里

| 发现                                                                                                                                                               | 证据                                           |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------- |
| **进程链。**TTI 之前有四个 Node 进程：启动器、CLI、CLI 的重新启动副本，以及用于更新检查的 `npm view`。第一个 CLI 加载完整个模块图（约 700 毫秒）后又重新启动自身。 | strace；改为单进程运行后 TTI −33%、RSS −240 MB |
| **编译缓存。**交互式与 `-p` 从不启用 Node 编译缓存，只有 `serve`/`mcp`/`--help`/`--version` 快速路径会启用。                                                       | 冷缓存运行与热缓存运行结果相同                 |
| **导入时的副作用。**10 次同步 `execSync('command -v <editor>')`；为遍历 IDE 祖先进程而进行的 7 次 `sh`+`ps` 启动；启动期间运行 `npm view`。                        | CPU 剖析中的调用者栈                           |
| **双字节 JS 源码。**注释中的 `—`/`─` 使 TTI 前 25 MB JS 中有 21 MB 为非 ASCII，V8 因此以双字节字符串存储；UTF-8 解码耗时 64 毫秒。                                 | 字节扫描；CPU 剖析                             |
| **模块图过大。**解析参数时会求值整个 `review` 命令树（1.5 MB）；虽然只注册了 `common`，却求值了全部约 190 个 highlight.js 语法。                                   | esbuild metafile 导入链                        |
| **页脚更新过晚。**页脚中的 git 分支在 TTI 之后才到达，Ink 随之重绘整个屏幕。                                                                                       | TTI 后的屏幕差异                               |

## 4. 目标与范围

**目标。**在不回退 S1 的前提下，改善全新启动时的 J1–J3 与 M1。

**范围内。**POSIX 上的普通交互式 TUI、`-i` 与 `-p`。

**保持不变：**

- ACP 与 IDE 集成；
- stream-json、`--json-fd`、文件输入与双路输出；
- 沙箱；
- Windows 的进程处理；
- `advanced.autoConfigureMemory=true`；
- `.env` 文件或 `settings.env` 注入了值的会话；
- Bun standalone 版本的启动器。

**本 PR 范围外：**

- 恢复会话与首轮延迟；
- 现场遥测；
- CI 门禁。

## 5. 改动（实验记录）

所有 Δ 均为相对前一版本的成对中位数；"成对"表示改动胜出的对数。

| #   | 改动                                                                                                                          | 结果                                                                                          | 决定            |
| --- | ----------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------- | --------------- |
| E1  | 当重新启动不增加任何 Node 参数时跳过它。信任变更后的重启改为原地 re-exec（`execve`）；`/update` 在进程内执行。                | TTI −767 毫秒（20/20）；首个内容 −707 毫秒；RSS −190 MB；`-p` −748 毫秒                       | 保留            |
| E2  | 在加载主模块图之前调用 `module.enableCompileCache()`                                                                          | 热缓存：TTI −358 毫秒（20/20），`-p` −296 毫秒，RSS +10 MB。冷缓存首次启动：TTI +104 毫秒     | 保留            |
| E3  | 编辑器对话框需要时才探测编辑器，而不是在导入时探测                                                                            | 首个内容 −47 毫秒（19/24）；TTI −32 毫秒（14/24）                                             | 保留            |
| E4  | 仅当 `TERM_PROGRAM=vscode` 时遍历 IDE 祖先进程                                                                                | TTI −50 毫秒（20/24）；CPU −120 毫秒；**S1 回退**，8/24 → 22/24                               | 与 E5 一起保留  |
| E5  | 在第一帧之前解析 git 分支                                                                                                     | S1 17/24 → 0/24；时间无变化                                                                   | 保留            |
| E6  | esbuild `minifyWhitespace`，`keepNames` 保持开启                                                                              | RSS −23 MB（24/24）；首个内容 −48 毫秒（21/24）；TTI −36 毫秒（17/24）；`-p` −43 毫秒、−21 MB | 保留（决定 3）  |
| E7  | 渲染后 3 秒才开始更新检查                                                                                                     | TTI −56 毫秒（20/24）；TTI 时 RSS −81 MB（24/24）；CPU −335 毫秒                              | 保留            |
| E8  | 在 POSIX 上的 Node 中，bin 启动器在进程内导入 CLI。`gc` 通过 `v8.setFlagsFromString` 暴露，退出码 44 由退出钩子负责重新启动。 | TTI −48 毫秒（20/24）；首个内容 −73 毫秒（23/24）；RSS −49 MB（24/24）；`-p` −48 毫秒、−50 MB | 保留            |
| E9  | 仅当 argv 中出现 `review` 时才注册 `review` 命令                                                                              | RSS −8 MB（24/24）；`-p` −14 MB；时间无变化                                                   | 保留            |
| E10 | 只加载 lowlight 的 `common` 语法（chunk 1.5 MB → 216 KB）                                                                     | RSS −1.5 MB；时间无变化                                                                       | 保留（改善 D2） |
| —   | 怀疑扩展存储锁在 `config.initialize` 中耗费 200 毫秒                                                                          | 锁无竞争：空隙来自繁忙的主线程，而不是锁                                                      | 不改动          |

### 5.1 重新启动

重新启动是最大的单项开销，却没有人说得清它的用途。

**它原本做什么：**

- **堆上限。**它会加上 `--max-old-space-size`，但只在 `advanced.autoConfigureMemory` 为 `true` 时才加。默认值为 `false`，因此默认情况下子进程不会得到任何额外参数。
- **监管。**父进程常驻只为处理三类事件：
  - 退出码 42：文件夹或 IDE 信任变更，重启会话；
  - 退出码 43：`/update` 请求重新启动；
  - IPC "退出时更新"消息。
- **环境变量交接。**它会传递哪些变量来自 `.env` 或设置。这只在跨进程边界时才有意义。

**改了什么。**在 POSIX 上，当没有内存参数、没有任何 `.env` 文件或 `settings.env` 注入值、且当前模式不需要单独的监管进程时，CLI 将自身标记为进程内监管：

- `relaunchApp()` 执行退出清理，然后以相同命令行 `execve`。如果 gc 是在运行时暴露的，则额外加上 `--expose-gc`。
- `relaunchForUpdate()` 在进程内运行更新处理函数，并以其返回码退出。当返回码为 44 时，启动器的退出钩子负责重新启动。
- 请求在退出时安装的更新，会在会话正常结束（退出码 0）后安装，且只安装一次，即使用户在此期间再次退出。安装成功时退出码为 44，由启动器的退出钩子打开新版本，与原先的监管父进程一致。
- 其他所有模式保留受监管的重新启动。由于启动器不再通过 argv 传递 `--expose-gc`，其子进程按同一规则获得该参数。

**env 文件。**在加载时读取环境变量的模块，以及读取 `NODE_EXTRA_CA_CERTS` 的 Node 本身，只有在全新进程映像中才能看到来自 `.env` 或 `settings.env` 的值。因此这类会话与 `main` 一样重新启动；`main` 对一次性运行也采用同一规则（#12602）。

**Bun。**Bun standalone 版本没有 `v8.setFlagsFromString`，因此其启动器与 Windows 一样，保留带 `--expose-gc` 启动的子进程。

## 6. 结果

基线 `main` @ 906418a 对比最终版本，30 个交错对，干净 HOME，4 vCPU：

| 指标             | 基线 p50  | 最终 p50  | 变化          | 胜出对数 |
| ---------------- | --------- | --------- | ------------- | -------- |
| J1 交互式 TTI    | 2546 毫秒 | 1179 毫秒 | −54%（2.16×） | 30/30    |
| J3 首个内容      | 1973 毫秒 | 746 毫秒  | −62%（2.65×） | 30/30    |
| J2 无头 TTFR     | 2149 毫秒 | 940 毫秒  | −56%（2.29×） | 30/30    |
| M1 TTI 时 RSS    | 574 MB    | 231 MB    | −60%          | 30/30    |
| M1 `-p` 峰值 RSS | 294 MB    | 218 MB    | −26%          | 30/30    |
| S1 TTI 后变化    | 12/30     | 0/30      | —             | —        |

**总体。**J1–J3 的几何平均为**快 2.36 倍**，RSS 低 2.48 倍。

**其他场景：**

- 编译缓存为空的首次启动：TTI −40%（15/15 对）。
- 不应答 OSC 11 的终端：TTI −49%（15/15 对）。

**独立复现**（见 PR 讨论）：

- Linux arm64，10 vCPU，30 对：J1 −49%，J3 −57%，J2 −53%，M1 −60%，S1 26/30 → 0/30；每一对都胜出。
- Apple Silicon 上的 macOS，8–12 对：J1 −42%，J3 −48%，J2 −45%，RSS −54%；每一对都胜出。

**确定性代理指标：**

| 代理指标         | 交互式         | 无头           |
| ---------------- | -------------- | -------------- |
| D1 Node 启动次数 | 4 → 1          | 3 → 1          |
| D2 加载的 JS     | 40.8 → 16.0 MB | 35.8 → 13.3 MB |
| 其他子进程       | 29 → 3         | 24 → 8         |

**D3。**最终版本 `-p` 为 81.95 亿条指令。基线至少为 93.7 亿条；这是下限，因为 Valgrind 会丢弃被 `execve` 替换掉的进程映像。

## 7. 风险与行为变化

1. **退出时更新**（standalone 与 `updateCommand` 安装）。保留：会话正常结束后安装更新，并由启动器打开新版本，与 `main` 一致。设置对话框中"需要重启"的退出不会安装更新。
2. **信任重启后再 `/update`。**原地信任重启之后，同一会话中再执行 `/update`，或请求在退出时安装的更新，会安装更新但以退出码 44 退出而不是打开新版本，因为新映像中没有启动器的退出钩子。这需要在同一会话中开启文件夹信任、发生信任变更并进行更新；下一次运行 `qwen` 即为新版本。
3. **堆栈。**启用 `minifyWhitespace` 后，堆栈帧保留函数名和精确的行号与列号，但每行平均约 2 KB，仅凭行号已无法定位到可读的代码。两个构建都不附带 source map。
4. **短会话。**更新检查会等待 3 秒，因此短于 3 秒的会话会跳过它；下一次启动时再检查。
5. **IDE 进程遍历。**现在只在 VS Code 终端中运行。这不会造成损失：版本低于 0.5.1 的配套扩展不会在连接文件中写入 IDE 身份，因此在会改写 `TERM_PROGRAM` 的终端（例如 tmux）中，`main` 同样无法为它们识别 IDE。
6. **编译缓存。**缓存位于操作系统临时目录（约 9 MB），安装或升级后的首次启动慢约 100 毫秒。启动器会导出 `NODE_COMPILE_CACHE`，因此与 `main` 一样，重新启动的子进程和工具子进程共享该缓存。
7. **进程链。**在 POSIX 上不再设置 `QWEN_CODE_LAUNCHER_PID`。只有 Windows standalone 更新器读取它，而 Windows 仍保留子进程方式。
8. **核心受控区域。**本改动涉及 `packages/core/src/**` 与 `packages/cli/src/config/**`，需要维护者审阅。
9. **env 文件会话。**当 `.env` 文件或 `settings.env` 注入值时，交互式与 `-p` 会话与 `main` 一样重新启动，保持原有的启动开销。
10. **Bun 版本。**其启动器保留子进程方式，因此得不到 E8 的收益。
11. **信号退出状态。**收到 SIGTERM 或 SIGHUP 时，进程以退出码 143 或 129 退出，而不是被信号杀死。shell 的 `$?` 相同；检查信号的父进程会看到正常退出。

## 8. 被否决的指标

| 指标                                   | 否决原因                                                                                      |
| -------------------------------------- | --------------------------------------------------------------------------------------------- |
| 外部帧数或写入次数                     | CV 为 12–18%，受 PTY 分块影响                                                                 |
| strace 的原始子进程数                  | 不可重复：`git` 调用次数会变化，`ps` 遍历次数取决于进程树深度                                 |
| Valgrind 下交互式的指令数              | 离散度约 10%，因为定时器驱动的工作量会随减速放大；需要一个在 `input_enabled` 时退出的基准钩子 |
| 以内部 `to_input_enabled` 作为总体指标 | 漏掉了用户实际等待中约 1 秒（35%）；仅保留作阶段拆解                                          |

## 9. 决定

以下事项在 PR 为草稿时尚未决定。

1. **退出时更新。**在进程内恢复，不再使用 "Run /update" 回退（第 5.1 节）。
2. **信任重启后再 `/update`。**作为有文档说明的边界情况接受（第 7 节第 2 项）。若在重启时 re-exec 启动器，就要处理 standalone 的 shell 启动器与托管版本锁定，代价超过这一边界情况本身。
3. **E6 堆栈。**保留。堆栈帧保留函数名与精确位置，两个构建都不附带 source map；source map 可以另行跟进。
4. **更新检查延迟。**保持渲染后 3 秒。它不再与启动争抢资源，更短的会话会在下一次启动时检查。
5. **IDE 遍历。**保留；不会造成损失（第 7 节第 5 项）。
6. **拆分。**作为一个 PR。若维护者倾向拆分，应拆出的是进程链改动（E1+E8）；评审中发现的问题都在这一部分。
7. **基准工具与 CI。**由后续 PR（#12674）提交供手动运行的基准工具，不设 CI 门禁。交互式的 D1 与 D2 截止于 TTI，而 TTI 之前由定时器驱动的工作会随 runner 速度变化，门禁会不稳定。

## 10. 验证

**构建与静态检查：**

- `npm run build` 与 `npm run typecheck` 通过。
- 改动文件的 lint 与 Prettier 检查干净。
- `npm run check:serve-fast-path-bundle` 通过。

**单元测试：**

- cli：涉及模块的 973 个测试；
- core IDE：119 个测试；
- `scripts/tests/cli-entry`：10 个测试。

新增测试覆盖：

- 进程内重启与更新；
- 重新启动路由；
- IDE 遍历门控；
- 分支预取；
- 更新检查延迟；
- 进程内启动器及其退出码 44 重新启动；
- gc 在运行时暴露时，两条重新启动路径都会带上 `--expose-gc`；
- env 文件的重新启动规则；
- Bun 下的启动器路径；
- 正常退出后安装退出时更新，且用户重复退出时只安装一次。

**集成测试：**

- 无需 API key 的套件通过（26 个文件，196 个测试）。
- 交互式套件除 4 个需要真实凭据的测试外全部通过；基线在同一环境下同样失败这 4 个。

**手动验证：**

- 操作真实的文件夹信任对话框：启动执行了两次，提示符恢复可输入，会话始终是单进程。
- 以退出码 44 退出的模拟安装会重新启动，参数保留、`gc` 可用、退出码正确传递。
- 在 `npm install -g` 安装上执行真实的 `/update`，由本地 registry 提供更新版本：更新被安装，并重新启动到新版本。
- 在以下启动模式中，会话进程都有 `global.gc`：交互式、`-p`、ACP、stream-json 以及 `advanced.autoConfigureMemory`。在 Node 22.23、Node 22.3（没有 `process.execve`）以及文件夹信任重启之后均成立。
- `qwen review --help` 正常工作。
- 在 pnpm 全局安装上验证退出时更新，由本地 registry 提供更新版本：执行 `/quit`、两次 Ctrl+C，或在关闭过程中第三次按 Ctrl+D 后，更新均被安装并打开新版本，与 `main` 一致。
- `~/.qwen/.env` 中有值时，交互式与 `-p` 会话会重新启动，并在启动时即可读到该值。
- 在 Bun 1.3.14 与 1.4.2 下，启动器能正常进入 CLI。

## 11. 验收标准

满足以下条件时，PR 即可就绪：

- 在干净环境中，J1、J2、J3 的 p75 相对 `main` 均至少改善 2 倍，M1 至少改善 50%，S1 为 0。
- 第 9 节的决定均已作出，并同步到本文档的两个语言版本。
- macOS、Linux、Windows 上的 CI 全部通过。

## 12. 后续工作

以下内容不属于本 PR：

- **更早渲染提示符。**`config.initialize()` 仍位于首次绘制与 TTI 之间（约 350 毫秒）。在它完成前就显示提示符属于设计层面的改动。
- **进一步精简模块图。**候选项：OpenAI 认证会话中的 `@google/genai`（581 KB），以及 daemon SDK 与 channels 代码（约 670 KB）。
- **修复误报通知。**全新 HOME 下会出现 "Extensions changed on disk"。
- **在应用内增加渲染提交计数器**，使"TTI 前的提交次数"可以成为门禁。
- **现场遥测。**启用已预留的 `qwen-code.startup.duration` 指标。
- **供手动运行的基准工具**（决定 7，#12674）。
- **打包堆栈的 source map**，如有需要（决定 3）。

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "cua-driver"
version = "0.17.0"
description = "Rust-backed Cua Driver SDK and bundled executable for client applications"
readme = "README.md"
license = { text = "MIT" }
authors = [
    { name = "TryCua", email = "hello@trycua.com" }
]
keywords = [
    "mcp",
    "computer-use",
    "automation",
    "accessibility",
    "screen-capture",
    "sdk",
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Operating System :: MacOS",
    "Operating System :: Microsoft :: Windows",
    "Operating System :: POSIX :: Linux",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Software Development :: Libraries :: Python Modules",
]
requires-python = ">=3.10"

dependencies = []

[project.optional-dependencies]
test = ["pytest"]

[project.urls]
Homepage = "https://github.com/QwenLM/qwen-code/tree/main/packages/cua-driver"
Documentation = "https://github.com/QwenLM/qwen-code/tree/main/packages/cua-driver"
Repository = "https://github.com/QwenLM/qwen-code"
Issues = "https://github.com/QwenLM/qwen-code/issues"

[project.scripts]
qwen-cua-driver = "cua_driver.__main__:main"

[tool.hatch.build.targets.wheel]
packages = ["src/cua_driver"]
artifacts = [
    "src/cua_driver/bin/*",
    "src/cua_driver/*.dylib",
    "src/cua_driver/*.so",
    "src/cua_driver/*.dll",
]
pure-python = false

[tool.hatch.build.targets.wheel.hooks.custom]
path = "hatch_build.py"

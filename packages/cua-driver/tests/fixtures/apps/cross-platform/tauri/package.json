{
  "name": "cua-test-harness-tauri",
  "version": "1.0.0",
  "private": true,
  "description": "cua-driver Tauri test harness loading the shared web DOM",
  "scripts": {
    "build": "cargo build --release --manifest-path src-tauri/Cargo.toml",
    "dev": "cargo run --manifest-path src-tauri/Cargo.toml"
  }
}
